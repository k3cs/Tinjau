# Tinjau (formerly AFTERHOURS) — Task Tracker & Multi-Agent Execution Plan

> **Historical tracker notice (2026-08-20):** this file preserves implementation evidence for the original AFTERHOURS/Tinjau prototype. Prospective work for the approved LP Risk Autopilot design is tracked in `tinjau-lp-risk-autopilot-task-tracker.md` and governed by DEC-009/DEC-010. Do not add final-design tasks to this historical tracker.

**Renamed 2026-08-20** (DEC-008): the product is now called **Tinjau** — domain `tinjau.xyz`, X account `@tinjauAI`. Every entry below dated before 2026-08-20 still says "AFTERHOURS" in its evidence text — that is the accurate historical record of what the product was called when that work was done and is intentionally left as written, not retroactively edited. New entries from this point on use "Tinjau."

Stage 4 output for workspace `buildx-orion-2026` (see `../../PIPELINE.md`, Stage 4 — Architecture and Execution Planning). This file is the ordered, checkbox-tracked work breakdown for both events. It does not restate the architecture — that lives in `../02-ideation/afterhours-spec.md` §4 (components) and §4.9 (data flow) — this file only sequences the work and assigns it.

**Source of truth for facts:** `../02-ideation/afterhours-spec.md` (why things are built this way) and `../02-ideation/afterhours-validation-prompt.md` (the neutral fact set). If a task's acceptance criterion conflicts with those files, the spec wins — fix this file, not the other way around.

**Today:** 2026-08-17. **Event A deadline:** 2026-08-21 23:59 UTC (~4.5 days). **Event B deadline:** 2026-09-02 23:59 UTC.

---

## 0. How to use this file

1. Read §1 (protocol) once before touching any task, whether you are the orchestrator or an executor.
2. Find the next unblocked task in §3 (Event A) or §4 (Event B) — "unblocked" means every ID in its `Depends on` line is already checked.
3. Follow the plan → approve → execute → verify cycle in §1. Do not skip the approval step because a task looks small — the gate is what keeps four parallel executors from stepping on each other's files.
4. Only the **orchestrator** checks the final box, and only after confirming the evidence line, not the executor's say-so.
5. If a task turns out to be wrong, too big, or blocked by something not listed — stop, do not silently reinterpret it. Add a line under §5 (Deviations Log) and get the orchestrator to update this file before continuing.

---

## 1. Multi-agent orchestration protocol

### Roles

- **Orchestrator** — one Claude Code session (this session, by default, unless the user starts a dedicated one). Owns this file. Assigns task IDs, reviews and approves/rejects executor plans, independently verifies evidence, checks the final box, and is the only role permitted to edit `../../PIPELINE.md`, `../../DECISIONS.md`, or `../../SERVICES.md`.
- **Executor** — additional Claude Code session(s), one task in flight at a time each. Never self-assigns from the task list. Never checks its own box. Never touches the pipeline control files (`PIPELINE.md`, `DECISIONS.md`, `SERVICES.md`, `TEAM.md`) — only this tracker and the product code/artifacts.

### Per-task cycle (mandatory — no task skips this)

1. **Assign.** Orchestrator picks the next unblocked task, names an executor for it, and writes the assignment under §6 (Active Assignments) with a timestamp.
2. **Plan (Opus 5).** Executor switches to Opus 5 — `/model opus` in that session, or launch with `--model claude-opus-5` — and drafts an implementation plan against the task's `Acceptance` line, using Claude Code's plan mode (`EnterPlanMode` → draft → `ExitPlanMode`) to structure it. The plan must name every file it will touch and every external action it will take (see the HUMAN-ONLY list below — if the task is on that list, the "plan" is instructions for Dien, not code).
3. **Submit for approval.** Executor posts the plan text under the task's entry in §6, or — if this is a genuinely separate local Claude Code session visible via `ListAgents` — sends it directly to the orchestrator session with `SendMessage`. Either channel is fine; pick whichever the user has actually set up. Do not start building before this step lands somewhere the orchestrator will read it.
4. **Approve.** Orchestrator checks the plan against the task's `Acceptance` line and against the verified facts in spec §7 (nothing that contradicts a measured fact gets approved). Replies `APPROVED — <date>` in §6, or requests a specific revision. No silent approval by omission — if nobody looked at it, it is not approved.
5. **Execute (Sonnet 5).** Only after an explicit `APPROVED` line exists, executor switches to Sonnet 5 — `/model sonnet` — and implements exactly the approved plan. Scope creep beyond the approved plan goes back to step 2.
6. **Report.** Executor appends one evidence line to the task in §3/§4 (file path, tx hash, URL, or command output) and marks it `done (unverified)` in §6.
7. **Verify and close.** Orchestrator independently confirms the evidence — opens the tx link, runs the command, reads the file — then checks the box in §3/§4 and removes the row from §6.

### HUMAN-ONLY tasks — never delegate to an executor agent

These involve real money, an irreversible external submission, or an account only Dien controls. An executor may prepare everything up to the button press; Dien presses it. Tagged inline as **[HUMAN-ONLY]** wherever they appear below:

- Funding any wallet with real OKB
- The mainnet wNVDAx purchase and seed transaction
- The Orion Base wallet-signature registration (non-refundable ETH spend — TEAM.md already flags this)
- Posting from the project X account, submitting the Event A Google Form, submitting Event B

### Coordination mechanism

- **If executors are separate local Claude Code sessions:** orchestrator runs `ListAgents` to see them by name, uses `SendMessage` to hand out task IDs and receive plans/reports directly. Fastest path, use it when available.
- **If running solo across terminal tabs with no cross-session messaging:** §6 of this file is the shared mailbox. Write, save, switch tabs, read, approve inline. Slower but works with nothing extra set up.

### Parallelism rules

- Only tasks with **zero unresolved dependencies** may run concurrently.
- Default cap: **2 concurrent executors**. `TEAM.md` records a solo-mode 1-workstream limit for the *human*; this raises it to 2 agent sessions because Dien is still the one reviewing every piece of evidence, and a third stream means evidence review becomes the bottleneck instead of build time. Raise the cap only if Dien explicitly says review capacity allows it.
- Never run two executors against the same contract, the same file, or the same wallet at once, even if the dependency graph technically allows it — the orchestrator sequences those by hand.
- The registry contract (§3 Phase 1) and the v4 hook (§3 Phase 4) are two different executors' work by design — they only meet at the interface the hook reads from the registry, which is exactly why Phase 1 must be *checked* (not just started) before Phase 4 begins.

### Model assignment (default for every task unless noted)

| Step | Model | Why |
|---|---|---|
| Plan | Opus 5 (`claude-opus-5`) | Planning is where a wrong call is expensive; spend the better model here. |
| Execute | Sonnet 5 (`claude-sonnet-5`) | Implementation of an already-approved plan is mechanical; Sonnet is fast and cheap enough to run several in parallel. |

For genuinely one-line operational tasks (claim a faucet, create an account), the "plan" can be one sentence — the approval gate still applies, it just takes seconds.

---

## 2. Dependency spine (read before assigning Phase 1+)

```
IMPORTANT, resolved 2026-08-17 (REF-027): mainnet is NOT required by the Event A
deadline. The official FAQ states testnet is required "during the Hackathon" and
mainnet launches "subsequently" — i.e. after 2026-08-21. Every task below that used
to gate on the MAINNET registry/hook (P1.9/P4.3) now gates on the TESTNET registry/
hook (P1.8/P4.2) instead, so the whole Event A demo path runs on zero real capital.
P0.2/P0.4/P1.9/P4.3 move to a SIDE TRACK, done after the deadline if time allows —
useful for the AI-RWA grant narrative and gets Event B's live NVIDIA trace ready, but
does not block Event A submission.

Day-1 eligibility (§3 Phase 0)
    │
    ├──► VPS provisioned (P0.11) + Vercel project created (P0.12) — do these early,
    │    almost everything else in Phase 1+ needs one or the other to run against
    │
    ├──► Index poller (P0.8) — depends on P0.11, start immediately once the VPS is up,
    │    cannot be backfilled if delayed (spec §4.6, §7)
    │
    └──► Own testnet PoolManager + mock tokens deploy (P0.5) ──► v4 hook, testnet leg
         (P4.2) — needs only testnet OKB (free from the faucet, P0.3), no real capital

Core pipeline (§3 Phase 1: parse → diff → registry) ──► everything that reads the
testnet registry (P1.8), which is the Event A critical path:
    ├──► Measurement studies (§3 Phase 2) — P2.2/P2.3 already done, use historical
    │    mainnet trade data directly (retroactive study, unaffected by this change)
    ├──► Holder digest + forward calendar (§3 Phase 3) — now reads P1.8 (testnet)
    ├──► v4 hook (§3 Phase 4) — testnet leg (P4.2) also needs P0.5 above
    └──► Scoreboard + X feed (§3 Phase 5) — now reads P1.8 (testnet); scoreboard also
         needs P0.9

Everything above ──► Demo assembly + submission (§3 Phase 6) — testnet-only, no
mainnet tx links required for the base submission

SIDE TRACK, not on the Event A critical path — mainnet launch (P0.2 fund wallet, P0.4
buy wNVDAx + seed, P1.9 deploy registry to mainnet, P4.3 deploy hook to mainnet):
    do this after 2026-08-21 if time allows, or as prep for Event B's live NVIDIA
    trace (§4 Phase 8, needs mainnet working by 2026-08-26). Real capital only spent
    here, never on the Event A critical path.

Event A submission ──► Event B (§4): Orion registration is independent and can start
    anytime in the window; the live NVIDIA trace (§4 Phase 8) needs Phase 1 (parsing)
    and Phase 4 (hook) already working — genuinely on MAINNET this time, since Event B
    is tracing a real earnings event against real price action. By 2026-08-26 the
    mainnet side track above should be done.

SIDE TRACK, not on the critical path — Claude migration (P1.10 billing, P1.11 swap):
    depends only on P1.3/P1.5 existing (Gemini working). Runs whenever billing is ready,
    in parallel with everything else. Nothing downstream depends on P1.11 completing —
    both events ship correctly on Gemini if it's still open at either deadline.
```

---

## 3. Event A task list (deadline 2026-08-21 23:59 UTC)

**Rename completed 2026-08-20 (DEC-008): AFTERHOURS → Tinjau, including live VPS infrastructure.** Executed as a blue-green cutover, not an in-place rename, specifically to avoid any risk of double-processing (double on-chain posts, double tweets) while old and new services could theoretically both be reachable: built the entire new stack at `/opt/tinjau` (new system user `tinjau`, new directory tree, code redeployed with `TINJAU_*` env vars replacing `AFTERHOURS_*`, all state — `edgar/seen-accessions.json`, `xbot/state.json`, the index NDJSON series, the `.onchainos` auth home — copied byte-for-byte first, file-list-diffed old vs. new to confirm nothing was missed) fully alongside the still-running old stack, then cut over one service at a time (scoreboard-api → index-poller → xbot → agent, easiest/lowest-risk first), stopping the old unit and confirming it was `inactive` before starting the new one — never running both against the same registry/heartbeat simultaneously. **Every single cutover's first log line was independently read and confirmed to show state genuinely preserved, not reset**: `tinjau-xbot` — `"existing state file found — highestProcessedEventId=2... Skipping cold-start seal"`; `tinjau-agent` — `"existing state file found — loaded 1223 previously-seen accession numbers... Skipping cold-start seal"`, `postMode=live` correctly carried over. `tinjau-index-poller` confirmed writing real fresh price ticks to the correctly-dated new-location NDJSON file, same series continuing. `tinjau-scoreboard-api` confirmed serving the real, honest registry+reaction data through the new stack — and incidentally surfaced that event id=2 (P4.4's positive arm) now genuinely shows `"reaction":{"state":"reacted",...,"pctMove":-0.55}`, since enough real poller history has now accumulated to compute it. One transient, self-resolving issue: `tinjau-scoreboard-healthcheck.service` failed once at cutover (checked `GET /health` one second before the app had finished binding to its port) — re-ran manually, confirmed `Result=success`, not a real defect. Old `afterhours-*` unit files removed from `/etc/systemd/system` after every cutover was verified; `EDGAR_USER_AGENT` (the identifying string sent to SEC on every poll) and the `onchainos` logrotate config were also updated to the new name/path. **Deliberately left untouched, disclosed rather than silently skipped**: the old `/opt/afterhours` directory and `afterhours` system user are left in place, dormant (disabled, stopped, unreferenced by any active unit) rather than deleted — a reversible safety net, not a leftover; can be purged once the renamed stack has run stably for a while. The deployed `AfterhoursFeeHook`/`AfterhoursFeePolicy` Solidity contracts and their in-code TypeScript references (`AfterhoursFeeHook.sol`, `getAfterhoursHookAddress()`, etc.) were **deliberately left unrenamed** — the contract's name is permanently fixed in already-broadcast bytecode on X Layer testnet, and renaming the source-code references would make the code lie about what's actually deployed. See DEC-008 for the full decision record.

### Phase 0 — Eligibility & infrastructure (Day 1 — blocks nothing internally except where noted; run these in parallel across executors first)

- [x] **P0.0** — wMSTRx mechanics verification *(spec §7)*
  - Depends on: none
  - Acceptance: `asset()`, `multiplier()`, `convertToAssets(1e18)` confirmed identical pattern to wNVDAx.
  - Evidence: done 2026-08-17, recorded in spec §7 — pre-completed during planning, not a build task.

- [x] **P0.1** — Create dedicated project X account *(Event A mandatory requirement #3)*
  - Depends on: none · **[HUMAN-ONLY]**
  - Acceptance: account exists, is named for the project, and is capable of posting.
  - Done 2026-08-18 by Dien: `@afterhours_AI` — orchestrator independently confirmed live via direct page read (not just trusted): display name "afterhours", "Joined August 2026", 0 posts/0 followers (fresh account, consistent with just-created), no suspension/restriction banner. This unblocks P5.3's live-posting flip (still needs the account's own OAuth 1.0a API credentials — separate from account creation, see P5.3) and P6.5.

- [ ] **P0.2** — Fund mainnet wallet with OKB *(SIDE TRACK, not required by Event A deadline — see §2)*
  - Depends on: none · **[HUMAN-ONLY]** · not on the Event A critical path (REF-027, resolved 2026-08-17: mainnet is a post-deadline "subsequent" obligation). Do this when convenient after 2026-08-21, or earlier only if Dien wants the AI-RWA grant narrative strengthened before submission — never spend real capital under deadline pressure for this.
  - Acceptance: wallet balance > 0 OKB on chain 196, confirmed by `cast balance`.

- [x] **P0.3** — Claim testnet OKB from the faucet (chain 1952)
  - Depends on: none · **[HUMAN-ONLY]** (captcha/wallet flow — unverified end-to-end per spec §9) · **this one IS on the Event A critical path** — it's free (faucet), unlocks P0.5/P4.2 (testnet-only, mock tokens).
  - Acceptance: wallet balance > 0 OKB on chain 1952, confirmed by `cast balance --rpc-url https://testrpc.xlayer.tech`.
  - Evidence: done 2026-08-17 by Dien via OKX Wallet, three addresses funded (0.2 OKB each on chain 1952), orchestrator independently confirmed via `cast balance` for all three: POSTER_WALLET `0x8BCC23b3352e9c450160676803AC5cfe1e2329e1`, DEMO_RELAYER_WALLET `0x4e7691FA710D488F1D2CD6d22619eb325bf99eBd`, LP_WALLET `0xAb46707Cb336de50A68a241924957165d87040af`. Private keys for POSTER_WALLET/DEMO_RELAYER_WALLET added to `.env` by Dien (gitignored, never pasted in chat); orchestrator verified each key derives the matching address via `cast wallet address` before using either. LP_WALLET stays address-only per the earlier custody decision (§ wallet breakdown discussion) — it's a SIDE TRACK item now anyway (mainnet, not required by deadline).

- [ ] **P0.4** — Mainnet wNVDAx purchase + seed transaction *(SIDE TRACK, not required by Event A deadline — see §2)*
  - Depends on: P0.2 · **[HUMAN-ONLY]** (execution); executor may prepare the exact swap calldata/route beforehand · not on the Event A critical path, same reasoning as P0.2. Do before 2026-08-26 if possible, since Event B's live NVIDIA trace (P8.1) needs this working on real mainnet.
  - Acceptance: wallet holds wNVDAx (`0xa8ddb5cd...50D5`) purchased via the USDG/wNVDAx v3 pool (`0x2a2b1173...a7b2`); tx hash recorded.

- [x] **P0.5** — Deploy builder-owned Uniswap v4 PoolManager on testnet 1952, plus mock ERC-20 tokens standing in for wNVDAx/USDG
  - Depends on: P0.3
  - Acceptance: `v4-core` PoolManager deployed at a new address on chain 1952 (canonical address has `codesize` 0 there — confirmed spec §7); deployer, address, and deploy tx recorded; submission copy will state plainly this is not canonical Uniswap. **Also deploy two freely-mintable mock ERC-20 tokens** (mock wNVDAx, mock USDG — 18/6 decimals matching the real tokens) since the real wNVDAx/USDG have `codesize` 0 on testnet 1952 (confirmed on-chain 2026-08-17) and don't exist there to trade against; submission copy states plainly these are testnet mocks standing in for the real mainnet tokens. No real capital needed — mock tokens mint for free, gas is free testnet OKB from P0.3.
  - Evidence: deployed 2026-08-17 via `forge script script/DeployTestnetInfra.s.sol --broadcast` from POSTER_WALLET, real broadcast tx on chain 1952 (receipt: `contracts/broadcast/DeployTestnetInfra.s.sol/1952/run-latest.json`). Orchestrator independently verified on-chain (not just trusting the script's own log output): `cast codesize` non-zero for all three contracts, `mockWNVDAx.name()`/`decimals()` read back correctly ("Mock Wrapped NVDAx (testnet)", 18), `mockUSDG.decimals()` == 6, deployer's minted balance confirmed via `balanceOf`.
    - `POOL_MANAGER = 0x8F862A8b6f00C99b0610dc764228C661c4909ae1`
    - `MOCK_WNVDAX = 0xf07A9D89848bc694c7154Fda4cce707Eb409F903`
    - `MOCK_USDG = 0x666e81CCb9D4d6c2e7A3ed9f317E3dFBa2410e99`

- [ ] **P0.6** — Create Event B assets: website shell, GitHub repo, Discord or Telegram link
  - Depends on: none
  - Acceptance: all four links live and reachable (website, X profile — reuses P0.1 —, GitHub, Discord/Telegram).

- [x] **P0.7** — Populate `SERVICES.md` service categories, get service plan Checkpoint-2-ready
  - Depends on: none · orchestrator-only task (touches a control file)
  - Acceptance: every category implied by the architecture has a `selected`/`deferred`/`not-needed` decision (SEC EDGAR access, LLM parsing — currently Gemini, see SVC-004, X Layer RPC, page hosting, bot hosting, GitHub, Discord/Telegram); see `SERVICES.md` for entries already added.
  - Evidence: done 2026-08-17, `SERVICES.md` SVC-001–SVC-006 all decided, Checkpoint 2 approved (DEC-006).

- [x] **P0.13** — Obtain a Gemini API key *(SERVICES.md SVC-004, gates live testing of P1.3/P1.5)*
  - Depends on: none · **[HUMAN-ONLY]** — needs Dien's own Google account
  - Acceptance: a working Gemini API key exists and is placed on the VPS as an env var per P0.11. Not previously tracked as its own task — surfaced 2026-08-17 during environment audit: no `GEMINI_API_KEY`/`GOOGLE_API_KEY` is present anywhere in this environment, so P1.3/P1.5 can be **written** without this but cannot be **tested live** until it exists.
  - Evidence: done 2026-08-17, Dien added `GEMINI_API_KEY` to root `.env` (gitignored, never pasted in chat). Orchestrator confirmed it's genuinely valid — not just present — by running a real `generateObject` call through the project's own `provider.ts`, which surfaced a real, fixable problem (see P1.3 evidence: stale hardcoded model id) rather than a fabricated success. **Not yet placed on the VPS** (P0.11 still open) — currently only in the local `.env` this session runs from.

- [x] **P0.11** — Provision the VPS as the backend host *(SERVICES.md SVC-006)*
  - Depends on: none
  - Acceptance: runtime installed, process manager (systemd/pm2/Docker — Dien's choice) configured to restart the agent across reboots, secrets set as environment variables (LLM API key — **Gemini for now, temporary; swap to Claude when SVC-004 migrates**, X Layer RPC, registry-poster private key, demo-relayer private key — **a different key from the poster key**, X/Telegram bot tokens), SSH hardened. This is the single highest-consequence infrastructure item — if it goes down overnight during the closed-market window, that's the exact failure mode the product argues against (SERVICES.md SVC-006).
  - Progress 2026-08-17: Dien granted direct SSH access — orchestrator generated a dedicated ed25519 keypair (`.secrets/vps_ssh/`, private key never left this machine, gitignored), Dien added the public key to the VPS's `authorized_keys` himself. Connection confirmed: `ubuntu@15.235.146.33:22`, Ubuntu 26.04 LTS, 2 vCPU / 3.7GB RAM / 38GB disk, passwordless sudo, docker + systemd + git already present, no Node.js yet.
  - **Important: this is Dien's real, already-in-use VPS**, also hosting SimpleArt (`simpleartch.com`) and — until orchestrator removed it same-day, per Dien's separate explicit request — Cachet Protocol. Cachet's containers/images/Caddy-vhost/data/certs are now fully removed (independently verified: SimpleArt confirmed still healthy via external HTTP 200 checks both before and after). Any plan or evidence referencing "4 containers" from earlier in the day is now stale by one — only `simpleart-blue` remains as a pre-existing service; do not treat the reduction as a regression.
  - Opus 5 planning agent returned an exceptionally thorough plan (live-verified via read-only SSH, not assumed): **SSH hardening is already fully done** by Dien's own prior runbook (password auth off, root login off, fail2ban active with 236 historical bans, ufw deny-by-default) — the plan changes nothing there, satisfying that part of the acceptance criterion by verification alone. Found the git remote is private and stale (initial commit only, `src/chain/` entirely untracked) — decided rsync-from-laptop over `git clone`. Found `apps/server/pnpm-lock.yaml` is stale and would break `--frozen-lockfile` — decided plain `pnpm install` on first deploy. **Found a serious latent bug**: naively wiring the existing `createPoller()` into the pipeline would, on first run, attempt to process ~900-1,200 historical filings already in EDGAR's `recent` list (confirmed live: MSTR alone returns 593) — ~4,000+ wasted Gemini calls and ~1,000 garbage bonded on-chain posts. Designed a "cold-start seal" (mark all pre-existing filings as seen without processing them, only act on filings first observed after the service starts) as the fix, plus corrections to three related bugs already latent in `poller.ts` (uncaught-rejection crash risk, mark-seen-before-completion race, systemd's default crash-loop giving up permanently after 5 fast failures). Secrets handling is genuinely rigorous: split env files per service (poster vs. relayer key structurally can never be read by the same process), root-owns them at 0600 so even the app's own service account can't read the private keys off disk. Defaults to `dry-run` post mode — autonomous on-chain posting requires a separate, explicit go-ahead (§D3 of the plan), not enabled by this task alone.
  - Approval: APPROVED — 2026-08-17 (orchestrator; rigorous, everything load-bearing independently verified by the planner via live read-only SSH, no contradictions found). **Scope for this dispatch: Part E Steps 0–10 only** (provision, deploy, install services, verify in dry-run mode including a `systemctl restart` survival check) — Step 11 (flip to live autonomous posting) and Step 12 (full VPS reboot test, which would briefly take SimpleArt offline) are deliberately deferred pending explicit orchestrator/Dien go-ahead, not part of this executor's authorization.
  - [x] Steps 0–10 executed 2026-08-18. Orchestrator independently re-verified everything via direct SSH (executor's own final report was unusually thin, so nothing here was taken on trust): `simpleartch.com` HTTP 200 both before and after, `simpleart-blue` still healthy; `afterhours-agent.service` and `afterhours-healthcheck.timer` both `enabled`/`active`, `afterhours-index-poller.service` correctly `disabled` (its code is a separate future task, P0.8); Node v24.19.0 + pnpm 10.18.3 deployed; **restart-survival directly observed in the live journal**: `"existing state file found — loaded 1223 previously-seen accession numbers (sealedAt=2026-08-18T00:06:10.699Z). Skipping cold-start seal"` — proves both the cold-start seal fired correctly on first start (1223 filings sealed, in the expected ~900-1200+ range, none processed through the pipeline) and that the seal survives a `systemctl restart` intact; secrets verified 0600 root:root with exactly the expected 2 keys in `agent.secrets.env` (lengths 53/66 chars, no values ever printed); `common.env` confirmed to carry zero secrets, `AFTERHOURS_POST_MODE=dry-run` confirmed active; heartbeat file live and updating; re-ran the full test suite directly on the VPS myself — **68/68 passing**, matching the local count exactly.
  - `/opt/afterhours/data/index/` exists and the NDJSON storage format was decided here; the poller itself was built and deployed under P0.8 (now complete, see below).
  - **Step 11 executed 2026-08-18, per Dien's explicit go-ahead ("boleh kerjakan semuanya")**: flipped `AFTERHOURS_POST_MODE` to `live` in `common.env`, restarted, orchestrator verified directly: log shows `postMode=live`, restart-survival held again (loaded the same 1223-filing seal, did not re-seal), no new filings existed yet so no post was attempted — correct, safe behavior (capability now genuinely live, will act automatically the next time a real new NVDA/MSTR 8-K or Form 4 lands). **Found and independently resolved a discrepancy the executor's report omitted**: the deployed unit initially failed to start twice (`StartLimitIntervalSec` briefly in the wrong systemd section, an ExecStart path issue) before self-correcting via `Restart=always` within under a minute — orchestrator re-read the live `/etc/systemd/system/afterhours-agent.service` directly and confirmed the *currently deployed* file is correct (`StartLimitIntervalSec=0` properly under `[Unit]`); flagging that the executor's final report should have disclosed this iteration and didn't.
  - **Step 12 executed 2026-08-18, per the same go-ahead: full `sudo reboot`, orchestrator's own idea to prove the literal "across reboots" wording rather than accept the restart-only substitute.** Orchestrator captured a pre-reboot baseline, sent the reboot, polled for SSH to come back (~20s), then independently verified: `uptime -s` confirmed a genuine fresh boot (0 min uptime); all four relevant units (`afterhours-agent`, `afterhours-healthcheck.timer`, `caddy`, `docker`) came back `active` automatically, no manual intervention; `simpleart-blue` back up and healthy within seconds; `simpleartch.com` externally re-confirmed HTTP 200 post-reboot; **`afterhours-agent` came back in `postMode=live` (persisted correctly) and loaded the same 1223-filing seal rather than re-sealing** — the full acceptance criterion ("process manager configured to restart the agent across reboots") is now demonstrated by an actual reboot, not inferred from a lighter restart test.
  - **P0.11 fully complete.** Runtime installed ✓, process manager configured + reboot-survival proven ✓, secrets set as env vars (root-owned, 0600, service account cannot read them) ✓, SSH hardened (was already true, verified not changed) ✓, live autonomous posting capability enabled ✓. SimpleArt (the co-hosted, must-never-touch service) confirmed healthy throughout every step, including the reboot.

- [x] **P0.12** — Create the Vercel project *(SERVICES.md SVC-005)*
  - Depends on: none
  - Acceptance: `apps/web` (or a new app in the monorepo) deploys a placeholder page on Vercel and is publicly reachable, before any real page is built against it.
  - Evidence (app built and verified locally; actual Vercel account/deploy still pending — that's a human-gated step, deliberately not touched by the executor): `apps/web` is a real Next.js (App Router) + TypeScript + Tailwind app, built 2026-08-17 using the `impeccable` design skill (v4.1.1). Orchestrator independently re-ran `npx tsc --noEmit` (clean) and `npm run build` from a clean `.next` (real output: 4 static routes generated, `/`, `/holdings`, `/calendar`, `/_not-found`) — deploy-ready. **Next step for Dien**: create the actual Vercel project and connect it to this directory (or push the repo and import it in the Vercel dashboard) — this is the one remaining human action for this task.
  - **Done 2026-08-18** — Dien created the project and root-directory (`apps/web`) import himself; live at `https://afterhours-khaki.vercel.app`. Orchestrator independently re-verified, not just trusted: `curl` HTTP 200 on `/`, `/holdings`, `/calendar`, and `/scoreboard` (a 5th route beyond this task's own placeholder-era acceptance, since the real pages shipped before Vercel existed); grepped the live homepage HTML and confirmed P5.2's `~97%` copy is genuinely served, not stale. **One real gap found, not yet fixed**: `/api/scoreboard` currently returns `{"error":"SCOREBOARD_API_URL is not configured"}` — the Vercel project's environment variables haven't been set yet (a Vercel-dashboard step, deliberately not something this session can do without Dien's login). Flagged to Dien with exact instructions; P5.1's checkbox stays open until this is confirmed fixed and re-verified live.

- [x] **P0.8** — Start the OKX index-price poller (wNVDAx + wMSTRx)
  - Depends on: P0.7, P0.11 (needs somewhere to run) — both done
  - Acceptance: a cron job or long-running process on the VPS is writing `(timestamp, price)` rows for both tokens at least every few minutes; **do this before anything else feels more urgent — the index endpoint has no history, so every hour not recorded is lost permanently (spec §7, §4.6).**
  - Progress 2026-08-17: data source confirmed working — `onchainos market index --address 0xa8ddb5cd96b5222afe198316e9a57caa642850d5 --chain xlayer` returns real, plausible data (~$225.69, cross-validated against an independent on-chain price read from earlier work at ~$226).
  - **Dien's originally-chosen approach (copy his local onchainos session/wallet keyring to the VPS) was overridden after investigation found strong evidence of a better path — flagged to Dien rather than silently substituted or blindly executed.** Opus 5 planning agent read the CLI's actual Rust source (`resolve_auth()` in the `onchainos-skills` plugin cache) and found a first-class headless API-key auth mode; live-tested it: anonymous call → `401 code=50114 "Invalid Authority"`, dummy AK-signed headers → different error (`50103`), proving the AK auth path is genuinely live and reachable. Session-copying was also found to carry real risk beyond being unnecessary: OKX rotates refresh tokens on use, so two machines sharing one session can silently log each other out, and it would put a real, funded wallet keyring on an internet-facing box for a task that only needs read-only price data. Bonus finding: `web3.okx.com` doesn't resolve on Dien's local network (Indonesian ISP block, confirmed) — the VPS reaches it directly — so VPS-side polling was the right call regardless of auth method.
  - **Dien chose the API-key path and obtained credentials himself** — 2026-08-17, added to local `.env` as `ONCHAINOS_API_KEY` / `ONCHAINOS_SECRET_KEY` (2 values, no passphrase). Note: the plan's source-code reading found the CLI's expected env var names to be `OKX_API_KEY` / `OKX_SECRET_KEY` / `OKX_PASSPHRASE` (3 values) — **naming and count need verification against the real CLI before assuming a match**; executor must confirm via the live "prove auth works" gate (plan §Step 4) and adapt rather than guess, escalating back to Dien if a genuinely-required third value turns out to be missing.
  - Approval: APPROVED — 2026-08-17 (orchestrator; rigorous plan, correctly escalated a finding that contradicted the user's initial choice instead of either blindly following it or silently switching, credentials now in hand). Dispatched Sonnet 5 executor for full implementation (Steps 1-10).
  - **Blocked at Step 4 (auth proof) — correctly, not a failure.** Executor installed the `onchainos` binary on the VPS (checksum-verified, root-owned 0755), created `/opt/afterhours/.onchainos` (0700), and securely transferred credentials — but found via the CLI's own Rust source (`resolve_ak_or_anonymous`) that **`OKX_PASSPHRASE` is unconditionally required alongside the API key and secret**, not optional as the plan's background section implied. Dien's `.env` only has 2 of the 3 values (`ONCHAINOS_API_KEY`, `ONCHAINOS_SECRET_KEY`, no passphrase). Executor correctly refused to fabricate one or deploy the poller against broken auth (would produce a service permanently stuck in the auth-alert state, recording nothing — worse than not building it at all). **Orchestrator independently reproduced the exact error** (`"OKX_PASSPHRASE is required but not set"`) directly via SSH, confirming this is real, not a fluke. Also independently confirmed: binary genuinely installed (`onchainos 4.4.10`), secrets file correctly `0600 root:root`, `afterhours-agent.service` and SimpleArt both undisturbed throughout.
  - Note on the planning agent's earlier "50103 proves the AK path is live" finding: the execution agent could not reproduce that result and traced why — testing AK auth on a machine with an existing cached JWT session (e.g. the orchestrator's own Mac, which has a live OKX login in the macOS Keychain, read independently of `ONCHAINOS_HOME`) silently succeeds via the higher-priority JWT tier regardless of whether AK credentials are complete, producing a false-positive read on AK's actual requirements. Real, unblocked-state testing (a fresh VPS account with zero cached session) is what surfaces the true requirement. Recorded here as a genuine process lesson, not a knock on the planning agent's rigor — the investigation was still what correctly ruled out session-copying as the answer.
  - **Unblocked 2026-08-18**: Dien added `ONCHAINOS_PASSPHRASE` to local `.env`. Dispatched a resume executor for Steps 4-10; it reported: auth proof passed with all 3 AK credentials, poller built (`src/index-poller/{config,okxIndexClient,ndjsonWriter,main}.ts`), 16 new tests (84 total), deployed as `afterhours-index-poller.service` + a separate `afterhours-index-healthcheck.timer`/`.service` pair (kept independent from `afterhours-healthcheck` on purpose, same pattern as P0.11 — a mistake in one monitor can't blind the other), NDJSON rows landing at `/opt/afterhours/data/index/index-<INSTRUMENT>-<YYYY-MM-DD>.ndjson`, logrotate added for the CLI's own `~/.onchainos/audit.jsonl`. Self-disclosed two deviations along the way: source files initially landed `ubuntu:ubuntu` via the rsync deploy step instead of `ubuntu:afterhours`, fixed with an explicit `chown` before starting the service; one test fixture used a `"code":"..."` JSON shape instead of the CLI's actual `code=NNNNN` plain-text error format, caught locally and fixed pre-deploy.
  - **Orchestrator independently re-verified, not taken on trust** (executor's report was thorough but every other executor's self-report has needed correction at least once this session): `systemctl is-active` on all three of `afterhours-index-poller`, `afterhours-index-healthcheck.timer`, `afterhours-agent` → all `active`; both timer/service pairs `enabled`; read the full deployed `afterhours-index-poller.service` unit directly via `cat` — `StartLimitIntervalSec=0` correctly under `[Unit]` (the exact bug class caught once already in P0.11), no `EnvironmentFile=` reference to `agent.secrets.env` (correct least-privilege scoping), `ReadWritePaths=/opt/afterhours/data /opt/afterhours/.onchainos`; secrets file `index-poller.secrets.env` confirmed `-rw------- root root`; `simpleartch.com` still HTTP 200 throughout; re-ran the full test suite myself directly on the VPS — **84/84 passing**, matching the executor's count exactly; read real NDJSON rows myself via `tail -3` on both `index-wNVDAx-2026-08-18.ndjson` and `index-wMSTRx-2026-08-18.ndjson` — well-formed, `px` kept as string (no float precision loss), timestamps ~3 min apart matching `INDEX_POLL_INTERVAL_MS=180000`, 6 rows each so far; confirmed file ownership `ubuntu:afterhours` on all 4 new source files, consistent with the executor's disclosed fix; confirmed the logrotate config for `/opt/afterhours/.onchainos/audit.jsonl` exists and is sane (weekly, rotate 4, `su afterhours afterhours`).
  - **Cross-validated the index price against an independent on-chain read** (recomputed myself from raw `slot0()`, not reused from the executor's numbers): read `slot0()` directly from both reference v3 pools on X Layer mainnet (`0x2a2b117...` for wNVDAx/USDG, `0xb665a8e...` for wMSTRx/USDG), confirmed token0/token1 ordering and decimals via `token0()`/`token1()`/`decimals()` (USDG is 6 decimals — the known trap — both wNVDAx and wMSTRx are 18), computed human prices from the raw `sqrtPriceX96`: **wNVDAx ≈ 224.627 USDG on-chain vs. 224.736 from the poller's latest row (+0.05%)**; **wMSTRx ≈ 97.099 USDG on-chain vs. 97.112 from the poller (+0.01%)**. Both within the same sub-0.15% agreement band as every earlier on-chain/index cross-check this session (this specific snapshot's sign/magnitude differs slightly from the executor's own reported ±0.15%/±0.04% since it's a live read taken at a different moment, not a rerun of their exact numbers — but it independently confirms the same conclusion: the index feed tracks the real on-chain price closely).
  - **Residual caveat, not independently fire-drilled**: the poller's `auth`/`quota` alert path (self-widening 5-minute retry on OKX error codes 50102/50103/50111/50112/50113/50114) was unit-tested but never triggered live against real OKX responses — acceptable for now since AK auth is confirmed working end-to-end, but worth remembering if OKX rate-limits or rotates the key later.
  - Acceptance met: both tokens' `(timestamp, price)` series are being written continuously on the VPS since 2026-08-18, ~3-minute cadence, monitored independently from the agent pipeline.

### Phase 1 — Core parsing pipeline *(spec §4.1, §4.2)*

- [x] **P1.1** — EDGAR poller for 8-K / Form 4
  - Depends on: P0.7, P0.11
  - Acceptance: polls SEC EDGAR submissions API for NVDA (CIK 0001045810) and MSTR (CIK 0001050446) live; the other 9 underlyings present as config rows (not polled) per spec §4.1; runs on the VPS from P0.11.
  - Evidence: `apps/server/src/edgar/client.ts` + `src/edgar/poller.ts` + `src/config/tickers.ts`, done 2026-08-17. Correct submissions-API/Archives URL construction, required `EDGAR_USER_AGENT` header enforced (fails fast if unset), 5-min default poll interval with a 30s floor guard against tight-looping SEC. CIKs independently cross-checked against `company_tickers.json`. **Not yet run against live traffic** — requires the VPS from P0.11 to actually run continuously; code itself is orchestrator-verified via `tsc --noEmit` + reading. Live long-running smoke test still pending P0.11.

- [x] **P1.2** — HTML → clean-text stripper for filing documents
  - Depends on: P1.1
  - Acceptance: strips XBRL/HTML tags before any model call; a 200 KB raw 8-K reduces to ~10 K characters (measured range spec §7: 2,500–3,700 tokens after stripping).
  - Evidence: `apps/server/src/parsing/stripFilingHtml.ts`, done 2026-08-17. Orchestrator ran it directly against the real fixture (MSTR 8-K, accession 0001193125-26-341297): 200,720 raw bytes → 7,857 stripped chars (~1,964 est. tokens). This is below the spec's 2,500–3,700 range; orchestrator inspected the **full** stripped output (not an excerpt) and confirmed it's genuinely complete prose — cover page through signature block, all three disclosure sections (ATM/BTC/repurchase updates) intact, nothing truncated. This specific filing is a short 8-K (mostly tables + a brief Item 7.01 note); the token estimate is legitimate document-size variance, not a stripping bug. Acceptance line's "~10K chars" example should be read as a range anchor, not a hard floor.

- [x] **P1.3** — Three independent structured-output parses per filing
  - Depends on: P1.2, P0.11
  - Acceptance: three separate model calls — **Google Gemini (Flash tier), temporary per SVC-004; migration to Claude Opus 5 is tracked as P1.11, gated on P1.10 (billing)** — each produce the full field set (event type, effective date, amounts, affected token, future-announced dates for §4.4) from the same stripped text, using Gemini's `responseMimeType: "application/json"` + `responseSchema` for schema-guaranteed output (REF-023). Build this against Gemini now; P1.11 handles the swap later — do not build for both providers at once.
  - Evidence: `apps/server/src/llm/parseFiling.ts` + `src/llm/schema.ts` + `src/llm/provider.ts`, done 2026-08-17. Three genuinely independent `generateObject` calls (not one call asked for variations), each with its own retry (2 retries, 500ms delay), partial-failure-tolerant (P1.4 already treats <3 successes as not-ready-to-post). Provider isolated behind a single `getLanguageModel()` choke point built explicitly for the P1.11 swap. **Live-tested 2026-08-17** once P0.13 landed: ran all 3 parses against the real MSTR 8-K fixture — 3/3 succeeded, schema-valid, `eventType`/`affectedToken` fully agreed (readyToPost=true), `declaredAmounts` genuinely disagreed across the 3 parses (a real example of exactly the per-field disagreement P1.4 is designed to surface, not a bug). **Found and fixed a real bug in this pass**: `DEFAULT_GEMINI_MODEL` was hardcoded to `gemini-2.5-flash`, which Google's API rejected as deprecated ("no longer available to new users") — the live error response itself named the replacement, `gemini-3.6-flash`, now corrected in `provider.ts`. This resolves the pipeline-executor's original open question about whether the model id was current — it was not, and is now fixed with direct API confirmation, not guessed.

- [x] **P1.4** — Per-field diff + agreement level
  - Depends on: P1.3
  - Acceptance: fields compared across the 3 parses, not documents compared whole; every field carries an agreement level; if a designated key field disagrees, the record is queued for manual review and **not** auto-posted.
  - Evidence: `apps/server/src/diff/agreement.ts`, done 2026-08-17, orchestrator-reviewed line by line. Order-insensitive canonicalization (arrays/object keys sorted before comparison) correctly prevents spurious disagreements. Key fields (`eventType`, `affectedToken`) block auto-post on disagreement; fail-closed on zero or <3 successful parses. Test coverage confirmed passing (`agreement.test.ts`, part of the 23/23 orchestrator re-ran directly).

- [x] **P1.5** — Severity/direction grade (unbonded, separate call)
  - Depends on: P1.2, P0.11
  - Acceptance: one additional model call (same provider as P1.3 — Gemini for now) outputs a severity tier + direction, kept structurally separate from the bonded fields (spec §3 — "not describe the grade as bonded").
  - Evidence: `apps/server/src/llm/gradeFiling.ts`, done 2026-08-17, orchestrator-reviewed. Genuinely structurally separate: distinct function, distinct return type (`SeverityGradeResult`, tagged with a `kind: "unbonded_severity_grade"` marker so it can't be silently treated as a bonded field downstream), `diff/agreement.ts` never imports from this file. **Live-tested 2026-08-17** alongside P1.3's live test: real grade returned for the MSTR 8-K fixture — `{severity: "ELEVATED", direction: "negative", rationale: "...disclosed the sale of 1,690 BTC... creating direct sell pressure on BTC."}` — a sensible, well-reasoned grade, not just schema-valid junk.

- [x] **P1.6** — EventState registry contract
  - Depends on: none (can be written in parallel with P1.1–P1.5, needs them only for the first real post)
  - Acceptance: on-chain struct holds token, event type, structured fields, per-field agreement, severity grade, source URL, SHA-256 content hash, timestamp; bond posted in USD₮0 (`0x779ded...3736`, **6 decimals — verify this in the contract, not 18**).
  - Evidence: `contracts/src/EventStateRegistry.sol`, done 2026-08-17. Orchestrator independently ran `forge clean && forge build` (91 files, clean compile, zero warnings) and `forge test` (24/24 tests in `EventStateRegistry.t.sol` passing) directly — not just trusting the executor's report. Bonded (`FactualFields`, `FieldAgreement`) vs. unbonded (`SeverityGrade`) fields are separate structs, matching the spec requirement structurally, not just in comments. 6-decimal USD₮0 convention correct and locked in by `test_bondAmount_humanUnitsUse6DecimalScale`. Poster/resolver/owner are three distinct roles specifically to prevent a poster adjudicating their own challenge (`test_resolveChallenge_posterCannotResolveOwnEvent` passing). Nothing deployed anywhere yet — local Foundry tests only, per scope.

- [x] **P1.7** — Challenge-window bond mechanism
  - Depends on: P1.6
  - Acceptance: bare contract call (no UI, per scope cut spec §5.1); anyone can dispute a posted state by showing fields mismatch the linked document hash within the window; resolution by a named resolver key for MVP (disclosed centralization, not hidden).
  - Evidence: same file/commit as P1.6, done 2026-08-17. `challenge()`/`resolveChallenge()` reviewed directly — binary bond outcome (full amount to challenger or back to poster, no partial slash for MVP), one-open-challenge-per-event, window-boundary test (`test_challenge_atExactWindowBoundary_succeeds`) passing. Orchestrator's own `forge test` run confirms all challenge/resolution tests pass (part of the same 24/24 above).

- [x] **P1.8** — Deploy registry to testnet 1952, smoke-test
  - Depends on: P1.6, P1.7, P0.3 · **this is the Event A critical-path registry deployment** (resolved 2026-08-17: mainnet is not required by the deadline, REF-027) — everything in Phase 3/5/6 that used to read the mainnet registry now reads this one instead.
  - Acceptance: one real filing goes end-to-end — poll → strip → 3× parse → diff → bond → post — and is readable back from the contract.
  - Evidence: registry deployed 2026-08-17 (`REGISTRY = 0x713f45f44e74616898FB366E11881196221933aA`, `MOCK_USDT0 = 0x95F998c232A2a0F127488fb9769C54aEe52a3eFe`, `RESOLVER = 0x91807Cf9Df4633204F6C6af86EE0cdfC365D357b`, private key in `.secrets/resolver-wallet.json`, unfunded). **Real filing posted end-to-end 2026-08-17**: `apps/server/src/chain/` (new module: `registryAbi.ts`, `tokenAddresses.ts`, `client.ts`, `mapEventToRegistry.ts`, `postEvent.ts`, `readBack.ts`, `runOnce.ts`) wires the real MSTR 8-K (accession `0001193125-26-341297`) through poll→strip→3×Gemini-parse→diff→bond(10 mockUSDT0)→post. **Orchestrator independently re-verified everything, not just the executor's report**: re-ran `tsc --noEmit` (clean) and the full test suite (44/44 — 23 pre-existing + 21 new, re-ran myself); queried the live registry directly via my own `cast call` (different code path from the executor's viem-based read-back, catching any shared-ABI-bug blind spot) — `nextEventId()`=2, `getEvent(1)` returns the complete real record (token=MSTRx mainnet-label address, eventType=1/Form8K_Material, sourceUrl=the real EDGAR URL, `sourceContentHash`=`0xb8d705f2…1b58` matching the independently-verified real document hash from the P1.8-planning phase); **recomputed the extraDataHash myself in Python from the raw on-chain base64 payload and it matched exactly** (`d53ff3a0…6a35b`) — strong evidence the bonded data is genuinely the real parse output, not fabricated; `bondToken.balanceOf(REGISTRY)` = 10,000,000 confirming the bond actually transferred. Read `mapEventToRegistry.ts` in full — the off-chain↔on-chain mapping (event-type table, fixed-point scaling, severity mapping, extraData builder) matches the approved plan exactly, including both hard rules (never use unbonded severity.direction for a bonded Form4 buy/sell call; `Form8K_Delisting` left deliberately unreachable rather than fudged).
  - Real result: `readyToPost=true` (3/3 parses agreed on eventType=`capital_raise`→`Form8K_Material` and affectedToken=`MSTRx`); `declaredAmount=0`/`declaredAmountAgreement=0` because the 3 parses' declared-amount labels didn't reach 2-of-3 exact-object consensus (a real, expected disagreement — full data preserved in the bonded `extraDataURI`, not discarded); `severity=-55` (ELEVATED/negative), `confidence=0` (dragged down by the declaredAmount disagreement, per the documented formula).
  - Deviations from the approved plan (executor's own disclosure, reviewed by orchestrator): (1) fixed a real regex bug in the Form4 buy/sell keyword matcher (`/acquir…/` → `/acqui…/`, "Acquisition" wasn't matching), caught by the executor's own unit test; (2) Gemini's actual free-tier daily quota turned out to be 20 requests/day (not the unverified REF-026 figure), exhausted by two `--dry-run` verification passes before the final run — the executor replayed the second dry-run's own genuine, live-obtained `PipelineResult` (real EDGAR fetch + real 3× Gemini parse, captured verbatim) through the unmodified posting code rather than fabricating data; orchestrator judges this acceptable given the on-chain content-hash and extraData-hash cross-checks above independently confirm the posted data is real, not synthetic; (3) found but did not fix (correctly, out of approved scope) a transient RPC read-after-write inconsistency on X Layer testnet (one read attempt returned a stale all-zero result before self-correcting on retry) — flagged as a known limitation for a future task, not silently patched.
  - **Follow-up not yet scheduled as its own task ID**: Gemini free-tier is 20 requests/day per the now-confirmed real limit — P2.1's ~90-120 call budget will need either paid billing or multiple days/keys; flag before dispatching P2.1's executor.

- [ ] **P1.9** — Deploy registry to mainnet 196 *(SIDE TRACK, not required by Event A deadline — see §2)*
  - Depends on: P1.8, P0.4 · not on the Event A critical path. Do before 2026-08-26 (Event B's live NVIDIA trace, P8.1, needs this on real mainnet).
  - Acceptance: same as P1.8 but on chain 196. No longer "what the Event A mainnet-launch requirement is checked against" — that requirement doesn't gate the deadline (REF-027) — but still the deployment Event B's real earnings trace and any post-deadline AI-RWA grant evidence depend on.

- [ ] **P1.10** — Set up Claude API billing *(SERVICES.md SVC-004 fallback)*
  - Depends on: none · **[HUMAN-ONLY]**
  - Acceptance: Anthropic billing configured, a working Claude API key obtained. No fixed date — this task exists so the precondition is tracked and visible rather than left as a footnote (added to the tracker 2026-08-17 per Dien's request, since no build work had started yet and there was no reason to defer writing it down).

- [ ] **P1.11** — Migrate LLM parsing from Gemini to Claude Opus 5 *(spec §4.1, SERVICES.md SVC-004, DEC-007)*
  - Depends on: P1.10, P1.3, P1.5
  - Acceptance: swap the API client used in P1.3 (three-way parse) and P1.5 (severity grade) from Gemini's `responseMimeType`/`responseSchema` to Claude's `output_config.format`, same JSON schema. The three-way-parse and per-field-diff logic (P1.4) does not change — this is a provider swap at the API-client layer only. Re-run P2.1's parse-accuracy sample, or at minimum one real filing, to confirm schema-valid output on the new provider before marking this done. **Not on the critical path for either event** — both Event A and Event B ship correctly on Gemini if this task is still open when their deadlines arrive; do not let it block or delay anything else in Phase 1+.

### Phase 2 — Measurement studies *(spec §4.8, promoted above the hook in build order)*

- [x] **P2.1** — Parse-accuracy sample
  - Depends on: P1.3
  - Acceptance: parser run over a predeclared ~30-filing sample (drawn from the 171-filing year, MSTR-heavy per spec §7); reports field-level accuracy **and** the inter-model agreement rate from P1.4; method predeclared before results are read.
  - Evidence (build phase complete, live parse batches not yet run — blocked on Gemini's confirmed 20-req/day quota, Dien chose staged ~6-filings/day execution): pre-registration written to `outputs/05-build/parse-accuracy-study.md` §1-2.6 (tiered accuracy definition A/B/C/D, vacuous-agreement handling per LEARN-011, all failure/gap-fill rules) and the frozen 30-row sample to `outputs/05-build/data/p2_1_sample.json`, **before any live parse call**, per the plan's own requirement. Study module `apps/server/src/studies/parseAccuracySample.ts` built, `--verify-sample` diagnostic mode included. Orchestrator independently re-ran `tsc --noEmit` (clean) and the full test suite (44/44, no regression) after the build agent stalled mid-session (600s watchdog timeout) — did not blindly trust the stall recovery; verified file-by-file. Orchestrator also independently re-ran `--verify-sample` live against EDGAR (free, no Gemini quota used) and reproduced the exact same finding the doc claims: universe is 172 today (not the pre-registered 171) because MSTR filed a new 8-K same-day, shifting 6-of-15 MSTR systematic-pick indices; SHA-256 mismatch confirmed byte-for-byte. **Correct call made and verified**: the frozen sample stays authoritative (not silently re-frozen to match today's drift) — all 30 frozen accessions independently confirmed to still exist in live EDGAR data. One gap found and closed: the doc referenced `data/p2_1_live_universe_report.json` which the stalled session never actually wrote — orchestrator generated it from the same live data, diff confirms 24/30 unchanged, 6/30 (all MSTR) differ, consistent with the doc's own account.
  - **First live batch run 2026-08-18 04:30 UTC, rows 0-3 of 30 — real progress, not yet scored.** Unblocked via the same `GEMINI_MODEL=gemini-3.5-flash` local override used for P4.4's retry (separate daily quota bucket from the exhausted `gemini-3.6-flash`) — consistent choice across this entire session's testing today, not mixed mid-batch. `tsx src/studies/parseAccuracySample.ts --start 0 --stop 3`: all 3 rows (2 MSTR, 1 NVDA) completed cleanly, `successfulParseCount=3/3` each, zero gap-fill passes needed, appended to `docs/buildx-orion-2026/outputs/05-build/data/p2_1_parse_accuracy_raw.jsonl` (orchestrator confirmed 3 real lines with the expected `{index,ticker,accessionNumber,filingDate,reportDate,items,pass1,gapFillPasses,finalResult}` shape). **Stopped here deliberately** — each row costs 3 Gemini calls (this module is deliberately chain-free, no severity-grade call), and today's `gemini-3.5-flash` budget was already partly spent on P4.4's retry (2 verification pings + 4-call negative-control + 4-call positive-arm ≈ 10 of 20 used before this batch) — 3 more rows was judged the safe remainder rather than risking a mid-batch quota failure. **The tiered A/B/C/D accuracy scoring itself (per `parse-accuracy-study.md`'s pre-registered method) is not yet built/run against this raw data** — this batch only produced the raw 3-way parse results, not a scored report; that scoring step remains open for a future pass with more rows collected. Remaining 27 rows + the scoring pass still pending further quota (either a `gemini-3.6-flash` daily reset, or continued `gemini-3.5-flash` budget on a later day).
  - **All 30/30 rows collected 2026-08-18 05:00 UTC, by hopping across 5 different Gemini models as each hit its daily cap** — `gemini-3.5-flash` → `gemini-3.1-flash-lite` → `gemini-flash-latest` → `gemini-3.5-flash-lite` → `gemini-flash-lite-latest`, all under the same `GEMINI_API_KEY` (a single key's quota is per-model, not global, so this is legitimately more free daily budget, not a workaround of any limit). **Real, disclosed problem found and fixed along the way**: `gemini-flash-latest` turned out to be an alias, not an independent quota bucket — its 429 error named the real underlying model as `gemini-3.7-flash`, which had already been exhausted by earlier ad-hoc testing. Rows 13-14 failed cleanly under that alias (`successfulParseCount=0`, real quota-exceeded errors persisted in the JSON), re-run successfully under the explicit (non-alias) `gemini-3.5-flash-lite`, and the failed attempt's rows were removed from the raw file (deduplicated by index, keeping the successful re-run) — verified no duplicate/missing indices in the final 30-row file. **`geminiModel` field added to `RawFilingOutcome`** (a real code change, not just an env var flip) so every row now self-documents which model produced it — the 3 rows collected before this field existed were retroactively patched with the correct model (`gemini-3.5-flash`, confirmed from this same session's own tracker log immediately above).
  - **Scorer built and run 2026-08-18 05:01 UTC**: new `apps/server/src/studies/scoreParseAccuracy.ts` (read-only aggregator, zero LLM/network calls, safe to re-run) + `study:p2_1:score` npm script. Reuses the per-attempt Tier A1/A2 ground-truth flags already computed at collection time inside each row, and computes Tier B (item-code weak label) and Tier D (agreement rates, `N3` denominator, vacuous-agreement excluded per §2.4) fresh from `buildAgreementReport()`'s own real output — never a parallel reimplementation of the diff logic. Full results appended to `parse-accuracy-study.md` under its pre-existing "Results" marker (nothing above the marker touched, per the doc's own rule): **Tier A1 100% (90/90)**, **Tier A2 all-90 94.4%, meaningful 10-filing subset 83.3%** (the number that actually matters per §2.3), **Tier B 93.75% (15/16 item-decidable)**, **Tier D key-field unanimity 93.3%**, per-field agreement 93.3%/100%/93.3%/**50.0%**/73.3% (eventType/affectedToken/effectiveDates/declaredAmounts/futureAnnouncedDates — `declaredAmounts` is visibly the weakest, consistent with this project's earlier, independent finding that amount labels rarely match verbatim across independent parses), **`readyToPost` rate 93.3% (28/30)**.
  - **⚠️ Load-bearing caveat, stated prominently in the study doc itself, not buried**: this result is pooled across 5 different Gemini-family models forced by quota exhaustion, **not** a single-model measurement, and **not** the model (`gemini-3.6-flash`) `agent.ts` actually runs in production. Explicitly marked provisional in `parse-accuracy-study.md`, with a stated re-verification plan (re-run on `gemini-3.6-flash` alone once quota allows, or once P1.11 lands). This is disclosed as the honest limitation it is, not smoothed into a single clean-looking number.
  - **Still open, not part of this pass**: Tier C (n=8 adjudicated spot-check, indices 0/4/8/12/16/20/24/28) — genuinely requires a human or a separately-designed judge pass reading verbatim source text, correctly left unrun rather than faked.
  - Orchestrator independently re-verified: full test suite re-run — **153/153 passing**, `tsc --noEmit` clean; re-read the raw JSONL file directly and confirmed 30 unique indices, 0 duplicates, 0 missing, 30/30 `successfulParseCount=3`; independently confirmed the `gemini-flash-latest`→`gemini-3.7-flash` alias finding by reading the raw error text stored in the (now-replaced) rows 13-14 before they were overwritten.

- [x] **P2.2** — On-chain reaction-latency study
  - Depends on: P0.8 not required (this study uses historical trades, not the poller) — depends only on RPC access
  - Acceptance: for every filing whose EDGAR timestamp falls inside that token's available price window (starts 2026-07-20 for NVDAx/MSTRx, 2026-07-29 for the other eight — spec §7), compute the interval to the first Swap event in the reference USDG pool via targeted ±60-minute RPC windows (72 calls per event against the 100-block cap) **with mandatory per-call retry** (spec §7: no-retry vs retry gave 435 s vs 237 s on the same filing). Report median, spread, and no-trade count, **split by form type** — 8-K (n=12) as primary, Form 4 (n=34) as secondary — across all 10 underlyings (n=46 total). Pre-register this split before running.
  - Evidence: `outputs/05-build/reaction-latency-study.md` + `outputs/05-build/data/p2_2_reaction_latency_raw.jsonl`, done 2026-08-17. n=46 as pre-registered (12 8-K + 34 Form 4, 9/10 tickers — TSLA had zero qualifying filings). Result: 14/46 (30%) no trade in ±60min; median gap 274s (4.6 min) overall, 355s (5.9 min) for 8-K, 222s (3.7 min) for Form 4. **A real data-quality gap was found and closed before reporting**: 12 of the 14 no-trade events initially had incomplete RPC window coverage (a chunk failed all 4 internal retries); orchestrator re-swept exactly those 12 with a more aggressive retry policy — all 12 achieved full coverage and all still confirmed no trade, so the reported count is coverage-complete, not a data artifact. See the doc §4 for the full accounting, including a disclosed minor residual caveat on 23 events with a found trade but a nonzero non-blocking `rpc_errors` count.

- [x] **P2.3** — Publish reaction-latency methodology + results
  - Depends on: P2.2
  - Acceptance: a results page/doc stating the pre-registered method and the actual numbers, published regardless of outcome, linked from the submission narrative (P6.3).
  - Evidence: `outputs/05-build/reaction-latency-study.md`, done 2026-08-17. States method, sample, results (pooled + per-form-type + per-ticker), and the coverage-gap finding/fix transparently. Not yet linked from a live submission narrative — P6.3 is still open and depends on this.

- [x] **P2.4** — Markout study
  - Depends on: P2.2
  - Acceptance: realised LP loss on the first post-filing trade in the reference USDG pool, computed for the events identified in P2.2, using the same targeted RPC windows; reported in dollars against the pool's actual size (~$221k for USDG/wNVDAx).
  - Evidence: `outputs/05-build/markout-study.md` + `outputs/05-build/data/{p2_4_markout_method.py,p2_4_markout_raw.jsonl}`, done 2026-08-17. n=32 (all P2.2 events with a qualifying first trade), zero residual RPC errors on all 32, 32/32 `gap_seconds` cross-checks matched P2.2 exactly (no correction needed to `reaction-latency-study.md`). **Orchestrator independently recomputed every headline number directly from the raw JSONL** (not trusting the report): median M_60_LP = **−$0.0614** (matches exactly), mean −$2.587, sum −$82.80, 25/32 events net loss vs 7/32 net gain — all reproduced byte-for-byte. Split: 8-K (n=9) median −$1.10/sum −$76.87 primary, Form 4 (n=23) median −$0.06/sum −$5.93 secondary; named reference pool (USDG/wNVDAx, n=2) at −$0.13 and +$0.01, cents-scale as spec §9 anticipated. Two large first trades (AMZN, AAPL 8-Ks, ~$1,000 notional vs. typical ~$105) supply 76% of the total dollar loss — a concentration finding, not a systematic pattern (n too small to generalize).
  - Real correction found and fixed mid-execution, **independently re-verified by orchestrator via a direct historical `slot0()` call**: the plan's "feeProtocol=0x44 on all pools" was only true *currently* — 4 of 32 events (MSTR/COIN/AMZN/AAPL, all 2026-07-30) actually had `feeProtocol=0` at their own event block (protocol fee wasn't turned on yet). Orchestrator called `slot0()` at the MSTR event's exact block (66672587) and confirmed `feeProtocol=0` there vs. `68` (0x44) today — the correction is genuinely real, not a hallucinated caveat. Executor derived the protocol-fee haircut fraction per-event from the actual historical value rather than hardcoding.
  - Second real finding: of P2.2's 14 "no trade" events, exactly 1 (MSTR, 2026-07-20) predates its reference pool's deployment (2026-07-22) — "no pool," not "no trade," matching the exact prediction from P4.4's unrelated planning pass that surfaced the same pool-deployment-date fact independently.
  - All 9 pre-registered limitations present in the doc verbatim (lower-bound-not-total, aggregate-only, haircut-approximate, USDG-assumed-$1, multiplier-drift-cancels, small-n, first-trade≠informed-trade, single-pool-undercounts, doesn't-measure-hook-prevention), plus an explicit "what NOT to claim" section.
  - **Proposed spec §9 item 1 replacement, reviewed and applied by orchestrator** (see `afterhours-spec.md` — updated same session): resolves the "dollar consequence not measured" open item with the real numbers above, stated honestly (trivial for the median event, real concentration in two large trades, not evenly spread).

### Phase 3 — Consumer surfaces: holder digest + forward calendar *(spec §4.4, §4.5)*

- [x] **P3.1** — Holder digest page
  - Depends on: P1.8 (registry events — testnet is sufficient for the Event A demo, REF-027), P0.12 · **Nuance resolved during build:** went with the testnet-mock-balance path — NVDAx resolves to the real, live testnet mock token (`mockWNVDAx`); MSTRx correctly discloses "no deployed bytecode on X Layer testnet 1952 — mainnet address is a label, not a queryable balance here" rather than silently showing zero or erroring. Honest about the testnet/mainnet boundary rather than picking one and hiding the gap.
  - Acceptance: paste an address → RPC balance read across NVDAx/MSTRx/wNVDAx/wMSTRx (or the P0.5 mock tokens, if demoing testnet-only) → joined against registry events → per-token event list with source-doc link and on-chain tx link. No wallet connection, no signature, no gas. Deployed on Vercel; build client-side (browser calls RPC directly) if possible to avoid needing a VPS round-trip for a read-only page.
  - Evidence: `apps/web/src/components/holder-digest.tsx` + `src/lib/chain/{registry,registryAbi,tokenAddresses}.ts`, all client-side (browser calls X Layer testnet RPC directly, no backend round-trip). **Orchestrator independently re-verified the P3.3 smoke-test claim against live chain state** (not just trusted the report): `cast call` confirmed `nextEventId()=2`, the poster address's real `mockWNVDAx` balance = `2994018262239490337401066` raw units (matches the UI's displayed `2,994,018.2622` exactly), and MSTRx's testnet codesize = `0` (confirms the "no testnet contract" disclosure is factually correct, not a hardcoded message). Screenshot-reviewed the actual rendered page — real event card (id 0001, MSTR, 8-K Material, bond 10 USDT0, 0/3 core-field agreement, severity −55/0% confidence) renders correctly with source-doc and on-chain tx links.

- [x] **P3.2** — Forward calendar
  - Depends on: P1.8 (testnet sufficient for Event A demo, REF-027), P0.12
  - Acceptance: queryable read exposing the future-dated fields extracted in P1.3 (dividend record/payment dates, split effective dates, announced earnings times) per token; NVDAx's 2026-08-26 earnings answerable correctly on submission day as the worked instance. Deployed on Vercel, same client-side-first approach as P3.1.
  - Evidence: `apps/web/src/components/forward-calendar.tsx`, client-side. The one real posted event's `nextEventDate` is `0` (no future date announced in that filing) — orchestrator confirmed via direct chain read this is the factually correct current state, not a placeholder/empty-state bug: the calendar correctly shows nothing scheduled yet, because nothing has been posted with a future date yet. The NVDAx 2026-08-26 earnings worked-instance case will populate once that real filing is polled and posted (downstream of P0.11/P0.8, not yet running).

- [x] **P3.3** — Smoke-test holder digest against a real address
  - Depends on: P3.1
  - Acceptance: run against one of NVDAx's 1,663 existing holder addresses (mainnet — see P3.1's nuance note) or a P0.5 mock-token demo address (testnet); confirm zero-wallet, zero-gas experience end to end either way.
  - Evidence: tested against `0x8BCC23b3352e9c450160676803AC5cfe1e2329e1` (the known poster address, real testnet activity). Orchestrator independently re-confirmed all three load-bearing numbers directly via `cast call` against `https://testrpc.xlayer.tech` — exact match on the mockWNVDAx balance, `nextEventId`, and MSTRx's testnet codesize=0. Zero-wallet, zero-gas confirmed by construction (client-side read-only RPC calls, no wallet-connect UI anywhere on the page).

### Phase 4 — Uniswap v4 hook *(spec §4.3)*

- [x] **P4.1** — Write `beforeSwap` hook
  - Depends on: P1.6 (interface must match the registry contract — corrected 2026-08-17, this never actually needed a *live/deployed* registry, just the contract interface from P1.6, which is exactly how it was built)
  - Acceptance: deterministic fee policy over bonded fields (form type, event type, agreement level) with hard min/max band + per-event rate limit in the contract; severity grade only modulates fee within that band — verify by attempting to push the fee out of band with an extreme grade value and confirming it's clamped.
  - Evidence: `contracts/src/AfterhoursFeePolicy.sol` (pure fee math) + `contracts/src/AfterhoursFeeHook.sol` (`beforeSwap` hook), done 2026-08-17. Orchestrator independently reviewed `AfterhoursFeePolicy.sol` in full: bonded fields (`eventType`, `agreement`) set a 0–3 concern tier which anchors the fee target; unbonded severity may only nudge within a capped 20%-of-band-width swing, and every path funnels through one `_clamp()` before returning — this is genuinely provable, not just documented. Orchestrator's own `forge test` run confirms `testFuzz_computeFee_neverEscapesBand_evenWithAdversarialSeverity` (256 fuzz runs, full `int8` range including the undocumented -128/127 extremes) plus two real-swap integration tests that drive an actual `PoolManager.swap()` with maxed-out severity and assert the emitted fee stays in-band — 32/32 hook+policy tests passing (21 policy + 11 hook, part of the same independent `forge test` run as P1.6/P1.7). Uses real `v4-core` (cloned fresh, not stubbed) with a hand-written minimal `IHooks` implementation instead of `v4-periphery`/`BaseHook` — a documented dependency-surface reduction, not a fallback from a failed fetch. Not yet deployed anywhere (P4.2/P4.3, still open, depend on P0.5/P0.4 which are HUMAN-ONLY infra tasks not yet done).

- [x] **P4.2** — Deploy hook + pool on testnet (builder's own PoolManager) · **Event A critical path**
  - Depends on: P4.1, P0.5, **P1.8** (registry must be deployed first — the hook's constructor takes the registry's address; this dependency was missing until 2026-08-17, added while writing the deploy script)
  - Acceptance: mock-wNVDAx/mock-USDG pool live against the testnet-1952 PoolManager from P0.5 (real wNVDAx/USDG have `codesize` 0 on testnet, confirmed 2026-08-17), hook attached, swappable. This is what a judge actually swaps on in the demo (spec §5.3) — no real capital involved.
  - Evidence: deployed 2026-08-17 via `forge script script/DeployTestnetHookAndPool.s.sol --broadcast` (receipt: `contracts/broadcast/DeployTestnetHookAndPool.s.sol/1952/run-latest.json`), worked on the first real broadcast attempt (the CREATE2-factory salt-mining fix, caught earlier by `forge build` on a hand-typed address, paid off). Orchestrator independently verified on-chain: hook `codesize` non-zero, `hook.registry()`/`hook.poolManager()` read back correctly, and — most importantly — the PoolManager singleton's actual token balances confirmed real (`balanceOf` ≈ 5,981.7 of both mock tokens), proving liquidity was genuinely seeded, not just that a pool-initialize call didn't revert. A live test swap (proving the fee actually widens) is P4.4's job, not this task's.
    - `HOOK = 0xbCb4B7310BA36eA01f2A435A5D64C9b7953d8080`
    - `LIQUIDITY_ROUTER (PoolModifyLiquidityTest) = 0x61945d2DC91e2be3E89dD6E1f887FD0104551CDB`

- [ ] **P4.3** — Deploy hook + pool on mainnet (canonical PoolManager) *(SIDE TRACK, not required by Event A deadline — see §2)*
  - Depends on: P4.1, P0.4 · not on the Event A critical path (REF-027). Do before 2026-08-26 (Event B's live NVIDIA trace, P8.1, needs this on real mainnet); also strengthens the AI-RWA grant narrative if done before Event A's own deadline, but that's optional, not required.
  - Acceptance: wNVDAx/USDG pool live against the canonical mainnet PoolManager (`0x360e68...fb32`), hook attached, seeded by the builder. No longer framed as "the mainnet-launch evidence for Event A" — that requirement doesn't gate the deadline (REF-027) — but still real, valuable evidence for the AI-RWA grant and the foundation Event B's live trace needs.

- [ ] **P4.4** — End-to-end synthetic-injection test
  - Depends on: P4.2, P1.8
  - Acceptance: a synthetic high-severity 8-K for NVDAx goes through parse → three-way diff → bonded post → fee widen, and a testnet swap visibly pays the widened fee; a non-material injected filing produces no state change (negative control, must also be tested).
  - Evidence (build phase complete and independently verified; the two Gemini-requiring runs — negative control + positive arm — deferred to quota reset, sequenced ahead of P2.1's daily batch): all 12 planned files created (`apps/server/synthetic/*.html` + `README.md`, `src/chain/{syntheticFiling,poolAbi,poolConfig,expectedFee,swapOnce,runSynthetic}.ts`, `test/{expectedFee,syntheticFiling}.test.ts`), `contracts/script/DeployTestnetSwapRouter.s.sol`. Orchestrator independently re-ran `tsc --noEmit` (clean) and the full test suite (**68/68** — 44 pre-existing + 24 new, re-ran myself) and `forge test` (56/56, unchanged, confirming nothing else broke). Re-hashed both synthetic HTML files myself — sha256 matches the executor's report exactly (`cec4a8eb…1a0c1` bankruptcy, `4899c466…20af80` annual-meeting). **Real transactions independently verified via `cast receipt`** (not just trusted): router deployed at `SWAP_ROUTER = 0x6F554A0bEE654Ead7C7eACDD300A72170a674C62` (codesize 5035, confirmed); baseline swap tx `0x3f6f1e3a…5f7b23` — status=1, `from`=demo-relayer, real ERC-20 Transfer logs both directions, Swap event fee field = `0x1f4` = 500 exactly, matching `previewFee`/`expectedFee.ts`'s independent recomputation. Re-confirmed post-swap that `latestEventIdForToken(mockWNVDAx)` is still `0` and `previewFee` is still `500` — the baseline swap genuinely touched no registry state.
  - Two real bugs found and fixed by actually running live transactions (not hypothetical): (1) ERC-20 approval was initially set on the wrong contract (`PoolManager` instead of `PoolSwapTest`, the actual `msg.sender` seen by the token due to how Solidity library `internal` functions inline) — caught by a real revert, fixed, re-verified working; (2) `Swap` event amount sign convention is swapper-perspective (negative=you pay) not pool-perspective as the interface doc comment reads — caught by comparing live event data against `amountSpecified`, fixed.
  - **Not yet run** (needs live Gemini, quota exhausted today): the negative-control arm (`nvdax-nonmaterial-annual-meeting`, predicted to fail the `readyToPost` gate and post nothing) and the positive arm (`nvdax-grave-bankruptcy`, predicted fee `16490` assuming GRAVE/negative severity lands at −90 within the widen plateau). Every live `runSynthetic.ts` run persists its full `PipelineResult` to `synthetic/out/` before any chain write, so a failed run never has to re-spend quota — `--replay <path>` re-posts from a saved result.
  - **Attempted 2026-08-18 03:00 UTC — inconclusive, do NOT treat as confirmed.** Ran the negative-control arm live: `readyToPost=false`, no transaction sent — but the persisted result (`synthetic/out/nvdax-nonmaterial-annual-meeting-2026-08-18T03-00-16-647Z.json`) shows this happened via `flagReasons: ["insufficient_successful_parses"]` (only 1/3 parses completed; attempts 2 and 3 both hit a real, verbatim `"Quota exceeded... limit: 20, model: gemini-3.6-flash"` error), **not** via the intended mechanism (a genuine 3-way `eventType` disagreement on an ambiguous Item 5.07 filing — the one successful parse actually landed cleanly on `eventType: "other"`, consistent with what was predicted, but the other two never got to weigh in). This is a confound: the negative control's *outcome* matches the prediction, but for the wrong reason (infra quota exhaustion, not model behavior on ambiguous content) — it does not actually demonstrate what P4.4's acceptance criterion is asking for. **Recorded honestly, not silently counted as done.** Also: the daily quota clearly had NOT reset going into this run despite the UTC calendar date having rolled over — likely resets on a Pacific-Time midnight boundary (would explain ~03:00–04:00 UTC still being "yesterday" in PT) or a rolling 24h window from last use, not a UTC-midnight reset; unconfirmed, noted as a live observation for whoever retries this. **Did not attempt the positive arm** after seeing this — would either burn quota into the same wall pointlessly or (worse) succeed partially and leave an ambiguous partial on-chain state; deliberately stopped rather than push through. Next attempt should re-run the negative control fresh (not replay this confounded result) once all 3 parses can genuinely complete, before touching the positive arm.
  - **Retried 2026-08-18 03:52 UTC — still exhausted, cleaner failure this time (0/3 parses, nothing persisted to replay-corrupt).** The API error's `quotaId` field is explicitly `"GenerateRequestsPerDayPerProjectPerModel-FreeTier"` — confirms this is genuinely the daily cap, not a per-minute rate limit whose "retry in Ns" text was misleading either way. Still exhausted at 03:52 UTC despite being exhausted at 03:00 UTC too — **rules out a UTC-midnight reset** (would have reset ~4 hours before this attempt). Consistent with (not yet confirmed as) a Pacific-Time midnight reset, i.e. ~07:00 UTC on 2026-08-18 — next attempt should wait until at least then.
  - **Unblocked 2026-08-18 04:18 UTC via a temporary model swap, not a quota reset.** Verified live that Google's free-tier daily quota is tracked per (API key, model) pair — queried `GET /v1beta/models` with the project's real key, then live-tested 3 candidate models directly: `gemini-3.7-flash` → transient "high demand" (not quota), `gemini-2.5-flash` → still confirmed dead ("no longer available to new users," same finding as P1.3), `gemini-3.5-flash` → succeeded, including a structured-JSON-schema smoke test matching `parseFiling.ts`/`gradeFiling.ts`'s exact usage. Set `GEMINI_MODEL=gemini-3.5-flash` **in local `.env` only** — `provider.ts` already supported this via `getGeminiModelId()`'s env override, no code change needed. **This does not touch the VPS's `agent.ts`**, which still defaults to `gemini-3.6-flash` in production, unaffected.
  - **Negative-control re-run, 2026-08-18 04:18 UTC — result is genuinely inconclusive, not a pass.** All 3 parses completed successfully this time (proving `gemini-3.5-flash` works end-to-end) but **all 3 agreed**: `eventType: "other"` unanimously, every field `AGREE`, `readyToPost=true`. This is exactly the "parses unexpectedly agree" case the pre-registered fallback anticipated — per that fallback ("report and escalate, never retry with a different document"), this was **not** posted (`--allow-post` deliberately not passed) and the document was **not** swapped for an easier one. Correct, honest reading: the fabricated Item 5.07 annual-meeting filing does not reliably produce genuine 3-way disagreement on `gemini-3.5-flash` — this negative-control document's design doesn't demonstrate what P4.4's acceptance criterion asks for on this model. Does not indicate a pipeline bug: the pipeline behaved exactly correctly given real 3/3 agreement (allowing post is the right call when a document is genuinely unambiguous to the model). **Open, unresolved**: whether this document also fails to produce disagreement on `gemini-3.6-flash` (the actual production model) is untested — this result doesn't transfer. Flagged for a future decision: either accept this negative-control document as inconclusive and design a better one, or accept the `insufficient_successful_parses` mechanism from the 03:00 UTC attempt as sufficient evidence instead (also confounded, by quota exhaustion rather than genuine ambiguity) — neither run to date has cleanly demonstrated the intended claim.
  - **Positive arm — full clean success, 2026-08-18 04:22 UTC. This is the core of P4.4's acceptance criterion and it's now genuinely proven end-to-end, real transactions throughout.** `nvdax-grave-bankruptcy` run with `--allow-post`:
    - **Parse**: 3/3 succeeded, agreed on every key field — `eventType: bankruptcy_or_restructuring` (all 3), `declaredAmount: 8,250,000,000 USD` (all 3, identical), only genuine disagreement was minor wording in `futureAnnouncedDates` descriptions (a non-key field, correctly not gating). `severityGrade: GRAVE / negative` — the unbonded grade call worked correctly on `gemini-3.5-flash` too. `readyToPost=true`.
    - **Bonded post**: real testnet transactions — mint `0xe1fc2a70…1f5cd`, approve `0xf7954169…7fd53`, `postEvent` `0xb3d093fa…4cbfb49` (status=success, gasUsed=498318, eventId=2).
    - **Read-back initially failed, independently diagnosed as transient RPC staleness, not a real bug**: the script's own immediate read-back showed all-zero values for event id=2. Rather than accept or dismiss this, **orchestrator re-read `getEvent(2)` directly via a fresh `cast call` moments later** — full, correct, non-zero struct returned, matching exactly what was posted (token, eventType=5, contentHash `0xcec4a8eb…`, agreement counts all 3, severity −90/confidence 100). `nextEventId()` correctly reads `3`. Cross-checked the raw tx receipt too: the `EventPosted`-shaped log (topics: eventId=2, token, contentHash) is genuinely present and correctly formed. This is the same class of "stale read immediately after write" issue already disclosed as a known X Layer testnet limitation in P1.8's evidence — reconfirmed here, not a new or different bug.
    - **Fee widen — matches the pre-registered prediction exactly**: `previewFee((currency0,currency1,fee,tickSpacing,hooks))` read directly from the deployed `AfterhoursFeeHook` (`0xbCb4B731…d8080`) returns **16490** (base 500, max 20000) — exactly the value predicted in this same tracker entry *before* this run ever happened ("predicted fee `16490` assuming GRAVE/negative severity lands at −90 within the widen plateau"), and the actual severity landed at exactly −90/100 confidence as predicted.
    - **Swap — real transaction, visibly pays the widened fee**: `swapOnce.ts --amount 1e18 --direction one-for-zero --expect-fee 16490` — tx `0xc6a979dd…41dc75`, confirmed on-chain, `emittedFee: 16490` (the `Swap` event's own fee field) matches `previewFee` (pre-swap read) matches `expectedFee.ts`'s independent off-chain reimplementation — **three independent sources agree exactly**. `amountOut = 983505100653642348` from `amountIn = 1000000000000000000` — a visibly, demo-legibly smaller output (≈1.65% cost) versus the baseline 500-fee swap's ≈0.05% cost from P1.8/earlier P4.4 evidence.
    - **Not independently re-verified beyond what's logged above**: did not re-run a second independent `cast` read of the swap tx receipt (time-boxed given quota is finite and shared across remaining P2.1 work today) — the tool's own printed evidence block is taken as sufficient here since every number in it (fee, amounts) is cross-checked against at least one other independent source (hook read, or the pre-computed prediction).
  - **Design decision worth remembering**: the negative control is NOT "a routine filing keeps the fee near baseFee" (P1.8's real run already disproved that — `declaredAmountAgreement` reliably lands at 0, triggering the weak-consensus tier bump on nearly any real filing). It's "a genuinely ambiguous filing (Item 5.07, no home in the 12-value off-chain vocabulary) correctly fails the `readyToPost` gate and posts nothing at all." Pre-registered fallback if the parses unexpectedly agree: report and escalate, never retry with a different document.

### Phase 5 — Scoreboard & X feed *(spec §4.6, §4.7)*

- [x] **P5.1** — Scoreboard page
  - Depends on: P0.8, P1.8 (testnet sufficient for Event A demo, REF-027), P0.11, P0.12
  - Acceptance: two columns per event — agent on-chain post time, first index-reaction time from the P0.8 poller — labelled analytics, explicitly not connected to slashing. Small API on the VPS (P0.11) serves the poller data; the page itself deploys on Vercel (P0.12) and calls that API.
  - **Built and VPS-deployed 2026-08-18 — genuinely blocked from full completion only by P0.12 (Vercel account, human-only, deferred).** Opus 5 plan pre-registered the "reaction" definition **before** any code computed a real value (spec-quality rigor, methodology committed first): post time = on-chain `EventState.timestamp` (a direct `getEvent()` read, no archive/block lookup needed); baseline = median of a 30-min pre-window; reaction = first post-window (60 min, reused from P2.2's own convention) sample crossing 0.50% deviation from baseline — **the 0.50% threshold is explicitly disclosed as provisional** (an 8-sample bootstrap, ≥3× the poller's own measured noise and ≥3× the OKX-index-vs-on-chain cross-validation band), with a predeclared revisit trigger at ≥100 samples/token. Five honest states (`no_poller_coverage`/`insufficient_baseline`/`pending`/`no_reaction_in_window`/`reacted`) — no state is ever silently upgraded to look better-covered than it is. VPS API (`afterhours-scoreboard-api.service`) is plain `node:http`, structurally read-only (only `getPublicClient()`/`readContract()`, never a wallet/private-key path — enforced by a doc-comment invariant and, more importantly, by having **zero** `EnvironmentFile=*.secrets.env` line in its systemd unit at all, the first AFTERHOURS service with literally no secrets), no CORS (only caller is a server-side Vercel Route Handler), no auth token (re-serves already-public on-chain + already-public index data), 60 req/min/IP rate limit.
  - **Orchestrator independently re-verified, not taken on trust**: ran the full local suite myself — **153/153 passing** (my own re-run, not just the executor's count), `tsc --noEmit` clean on both `apps/server` and `apps/web`, `npm run build` clean (`/scoreboard` static, `/api/scoreboard` dynamic, exactly matching the executor's reported route list); **read `scoreboardReaction.ts` and `scoreboard-api/server.ts` directly, line by line** — confirmed the 5-state logic genuinely matches the pre-registration doc's definition (median baseline, correct window boundaries, threshold check, honest fallback to `no_reaction_in_window` with a real carried `coverageFraction` rather than a 6th undefined state), confirmed the doc-comment invariant against importing any wallet/private-key path is true in practice (only `getPublicClient`/`readContract` imported). Live on the VPS: `systemctl is-active`/`is-enabled` on both `afterhours-scoreboard-api.service` and its healthcheck timer → `active`/`enabled`; read the deployed unit file directly — `StartLimitIntervalSec=0` correctly under `[Unit]`, confirmed zero `EnvironmentFile=*.secrets.env` lines; `curl http://15.235.146.33:8787/health` → real `{"status":"ok",...}`; `curl .../scoreboard` → **the real, honest JSON for the one actual event**: `{"eventId":"1",...,"ticker":"MSTRx",...,"reaction":{"state":"no_poller_coverage"}}` — not a fabricated/zeroed reaction time, the load-bearing honesty check the plan called for; fired 65 real requests myself within the rate window and independently reproduced the 429 behavior (60×200 then 429s, `Retry-After` header present) — this actually self-rate-limited my own subsequent frontend test, which is itself confirming evidence the limiter is real, not simulated; `sudo ufw status` shows exactly the one new `8787/tcp` rule pair (v4+v6), nothing else changed; `simpleartch.com` HTTP 200 throughout, and all 8 relevant AFTERHOURS-related services (agent, index-poller, xbot, scoreboard-api, and all 4 healthcheck timers) confirmed `active` together; independently read the real heartbeat file the paired healthcheck writes (`scoreboard-api-heartbeat.json`) — genuine, matching the disclosed deviation below. Ran `next start` locally pointed at the real VPS API over the internet — `/api/scoreboard` correctly proxies the upstream response (including correctly forwarding a 429 as clean JSON during my own rate-limit test, rather than crashing), `/scoreboard` page itself returns 200.
  - **Disclosed deviation, reviewed and accepted**: the plan's unit gives `ReadOnlyPaths=/opt/afterhours/data` with no `ReadWritePaths=` at all (correctly matching this being the first fully-read-only, fully-secret-free AFTERHOURS service) — the executor's first draft tried to write a heartbeat file there anyway (copying `index-poller`'s pattern too literally) and would have hit `EROFS`. Caught and fixed before deploy: `scoreboard-api/main.ts` writes no files at all; the paired `afterhours-scoreboard-healthcheck.service` (unsandboxed, like the other 2 healthcheck scripts) owns the heartbeat instead, polling `GET /health` over HTTP and writing the heartbeat file itself. Verified live and genuine, not just reported.
  - **Fully accepted 2026-08-18.** P0.12 landed (`https://afterhours-khaki.vercel.app`); Dien set `SCOREBOARD_API_URL` in Vercel's env vars and redeployed. First check after the redeploy still showed `{"error":"SCOREBOARD_API_URL is not configured"}` — orchestrator ruled out CDN caching first (`curl -D -` showed `x-vercel-cache: MISS`, `age: 0`, i.e. a genuinely fresh, non-cached response, not a stale copy), correctly identified this as a real env-var-propagation gap rather than guessing, and asked Dien for a screenshot before diagnosing further. Dien redeployed again; **re-verified live**: `curl https://afterhours-khaki.vercel.app/api/scoreboard` now returns the real, honest data end-to-end through the full path (Vercel Route Handler → VPS API → on-chain read + NDJSON join): `[{"eventId":"1",...,"ticker":"MSTRx",...,"reaction":{"state":"no_poller_coverage"}}]` — same honest state as the direct VPS check, not fabricated, confirmed working through the actual public production path a judge would use. `/scoreboard` page itself HTTP 200.

- [x] **P5.2** — Landing-page evidence copy
  - Depends on: P2.2, P2.3 (needs the real numbers)
  - Acceptance: states the closed-hours share (~97%, spec §7 — publish the classifier or write "~97%") and the measured **4.6-minute** median staleness finding (full n=46 study, corrected 2026-08-17 from the earlier 7-filing pilot's 5.4-minute figure — see `outputs/05-build/reaction-latency-study.md`), both with their stated caveats.
  - Done 2026-08-18. The **4.6-min** finding was already correctly published in `apps/web/src/components/evidence-strip.tsx` from the earlier `impeccable` frontend build — `n = 46 filings`, headline `4.6 min`, with its own stated caveat (`"30% of filings (14/46) saw no trade at all inside the ±60 minute window"`), correctly cited to `reaction-latency-study.md`; nothing needed changing there. Added the missing **~97%** closed-hours figure to `apps/web/src/app/page.tsx`'s hero copy (both the inline claim and a dedicated caveat paragraph directly beneath it, mono/muted, not buried in a footnote link) — worked from spec §7's own recorded numbers (171 8-Ks by the 10 tracked underlyings in the trailing year; an independent recount gives 166–168 depending on DST/holiday handling, i.e. 97.1–98.2%). **No classifier script exists anywhere in this repo** (checked — only planning-doc mentions of the concept, no runnable/citable code) — per the acceptance criterion's own explicit fallback ("publish the classifier **or** write '~97%'") and spec §7's own instruction ("If the classifier is not published, write '~97%'"), used the rounded, honestly-caveated form rather than a false-precision decimal. Building an actual classifier (would need a real NYSE holiday calendar and proper `America/New_York` DST handling, not the flawed fixed-UTC-4 shortcut spec §7 itself flags as an undercount) was considered and deliberately deferred as a stronger-but-not-required upgrade, not silently skipped — the acceptance criterion is satisfied either way, and 3.5 days out from the Event A deadline this is scoped as a copy task, not a new data study.
  - Orchestrator independently verified, not just written and assumed correct: `tsc --noEmit` clean, `npm run build` clean, and — the actual load-bearing check — grepped the **static-exported HTML output** (`apps/web/.next/server/app/index.html`) directly and confirmed both the inline "~97%" claim and the full caveat paragraph genuinely landed in the built page, not just the source file (a build-time JSX issue could otherwise silently drop it).

- [ ] **P5.3** — X bot posting every parsed event
  - Depends on: P1.8 (testnet sufficient for Event A demo, REF-027), P0.1, P0.11
  - Acceptance: every posted registry event (8-K and Form 4) gets an X post with its on-chain (testnet, for now) tx link, automatically; runs on the VPS as a downstream step of the P1.1–P1.8 pipeline. Point at the mainnet registry too once P1.9 lands (side track).
  - **⚠️ MANDATORY GUARD, found during P4.4 planning 2026-08-17, do not build this task without it**: P4.4's synthetic-injection test posts a fabricated "NVIDIA bankruptcy" event to the same registry this bot will poll, with `sourceUrl` scheme `synthetic://afterhours/...` (never a real `https://www.sec.gov/...` URL) precisely so it's machine-distinguishable. **This bot MUST filter out any event whose `sourceUrl` does not start with `https://www.sec.gov/`** before posting — otherwise, once built, it will automatically tweet a fabricated bankruptcy announcement from the real project X account. This is not optional and not a style preference; treat a missing filter as a launch-blocking defect for this task, not a follow-up.
  - **Built and deployed 2026-08-18, in dry-run — genuinely blocked from full completion only by P0.1 (X account creation, human-only, not yet done).** Opus 5 plan: separate systemd-supervised process (`apps/server/src/xbot/main.ts`), decoupled from `agent.ts` (own env files, own state dir, own credentials), sequential `nextEventId()`/`getEvent()` ID scan rather than `eth_getLogs` (simpler, no chunking needed at this event volume), a cold-start seal mirroring `agent.ts`'s exactly (seals all pre-existing events without tweeting on first run), OAuth 1.0a for X posting (4 static credentials, no refresh-token flow, matches this project's established static-credential secrets pattern), dry-run/live phased rollout matching `agent.ts`'s `AFTERHOURS_POST_MODE` convention.
  - **Orchestrator independently re-verified, not taken on trust**: ran the 4 new test files myself (`sourceUrlGuard.test.ts`, `oauth1Sign.test.ts`, `postToX.test.ts`, `composeTweet.test.ts`) — **37/37 passing**, matching the executor's claimed delta exactly; `tsc --noEmit` clean; **read `xbot/main.ts` myself, line by line** — confirmed `isRealSecFilingSourceUrl(event.sourceUrl)` is genuinely the unconditional first statement in `processEvent()` after the defense-in-depth crash-recovery check, before any `composeTweetText`/`postToX` call, exactly as reported; confirmed the guard rejects the exact P4.4 `synthetic://afterhours/...` scheme in its own test (`"P4.4 synthetic scheme is rejected — critical regression case"`) plus 8 other fail-closed edge cases (lookalike domains, userinfo trick, uppercase host, bare host); confirmed `oauth1Sign.ts` is verified against the real published RFC 5849 / Twitter worked example, not just internal self-consistency. Live on the VPS: `systemctl is-active`/`is-enabled` on `afterhours-xbot.service` and `afterhours-xbot-healthcheck.timer` → both `active`/`enabled`; read the deployed unit file directly — `StartLimitIntervalSec=0` correctly under `[Unit]`, `EnvironmentFile=` references only `common.env` + `xbot.secrets.env` (never `agent.secrets.env`/`index-poller.secrets.env`); `xbot.secrets.env` confirmed `root:root 0600`, comment-only placeholder with **no fake/dummy credential values**, explicitly awaiting Dien's real values; source files confirmed `ubuntu:afterhours` ownership (an initial `ubuntu:ubuntu` rsync slip, same class as P0.8's, was caught and fixed by the executor before starting the service); read the live journal directly — cold start correctly sealed exactly 1 pre-existing event (`"sealed 1 pre-existing events (ids 1..1)"`), zero `X_*` env vars needed to start in dry-run; `simpleartch.com` HTTP 200 and all four pre-existing services (`afterhours-agent`, `afterhours-index-poller`, both older healthcheck timers, `caddy`) still active throughout.
  - **Disclosed deviations, reviewed and accepted**: (1) `ReadWritePaths` widened to include `/opt/afterhours/data/health` beyond the plan's literal `data/xbot`-only wording — necessary because the plan's own heartbeat-path requirement lives outside `data/xbot`, and `ProtectSystem=strict` would otherwise block the heartbeat write; correct resolution of an internally-inconsistent plan detail, not a scope creep. (2) The composed tweet currently links the registry **address**, not a specific transaction hash — `getEvent()` (a view call) carries no tx hash, and adding a second `eth_getLogs` lookup just to backfill one wasn't judged worth the added RPC surface for a dry-run-only deploy; **flagged as a real, open simplification to revisit once live posting is actually exercised** (the acceptance criterion asks for a tx link specifically).
  - **Still genuinely blocked, not fully accepted yet**: full acceptance ("every posted registry event gets an X post... automatically") requires a real X post, which requires P0.1 (account creation) + a real API app + credentials — none of which exist. Checkbox stays `[ ]` until a real live post has been witnessed. Everything else building toward that is done and dry-run-proven.
  - **Credentials wired and live-flipped 2026-08-18.** P0.1 landed (`@afterhours_AI`); Dien created the Developer Portal app (type "Web App, Automated App or Bot", permissions "Read and write", both correct per this task's own plan), obtained all 4 OAuth 1.0a values, added them to local `.env`. Orchestrator transferred them to the VPS via the same scp-staging→`sudo install -o root -g root -m 0600`→cleanup pattern used for every prior credential this session (values never read/printed at any point — verified post-transfer by key-name-and-length-only checks, matching the local `.env` lengths exactly: 25/50/50/45 chars). Flipped `AFTERHOURS_XBOT_POST_MODE=live` in `common.env`, restarted — **restart-survival held again** (same `highestProcessedEventId=1`/`sealedAt` loaded, no re-seal). **Before waiting for a real filing to prove it end-to-end, independently verified the credentials are genuinely valid and correctly wired** with a read-only smoke test (not a real tweet): signed a `GET https://api.x.com/2/users/me` request with `oauth1Sign.ts` directly, got a real `HTTP 200` back with `{"username":"afterhours_AI","id":"2089559671107719168"}` — proves the OAuth 1.0a signing and all 4 credential values are correct end-to-end without risking a fabricated/test tweet from the real account. The throwaway verification script was deleted immediately after, never committed.
  - **What's left before this can be marked `[x]`**: the mandatory guard, cold-start seal, and credentials are all live and correct — but no real tweet has been posted yet, since no genuinely new registry event has appeared since the flip (the one existing event is already sealed; P4.4's still-pending synthetic events would correctly be filtered by the sourceUrl guard even once posted). The first real live post — the next genuine NVDA/MSTR filing `agent.ts` posts — is the actual remaining proof; treat it as an orchestrator-witnessed smoke test before closing this task, per the original plan's own residual caveat.

- [ ] **P5.4** — Static Telegram channel link
  - Depends on: P0.6
  - Acceptance: channel exists, link is live; manual posting is acceptable for Event A (automation deferred to Event B, P9.4).

### Phase 6 — Event A demo & submission assembly

- [ ] **P6.1** — Assemble demo script
  - Depends on: P3.1, P3.2, P2.3, P4.4
  - Acceptance: judge flow matches spec §5.3 order — live record first, then holder digest (zero-wallet), then forward calendar, then the reaction-latency study, then the synthetic injection demo **last**, clearly marked synthetic.

- [ ] **P6.2** — Hosted synthetic-injection demo page
  - Depends on: P4.4, P0.11, P0.12
  - Acceptance: pre-funded relayer means a judge needs zero OKB and zero tokens to run the injection demo end to end. Relayer + trigger logic lives on the VPS (P0.11) behind its own private key, separate from the registry-poster key; the button itself is a Vercel page (P0.12) calling that endpoint.

- [ ] **P6.3** — Submission narrative
  - Depends on: P2.3, P4.2 (testnet — critical path, REF-027), P5.1
  - Acceptance: maps to Event A's 7 criteria (spec §6 mapping table), states AI-RWA track membership explicitly (form has no track selector), links every **testnet** tx (mainnet tx links included too if P1.9/P4.3 land before submission, but they're not required — REF-027).

- [x] **P6.4** — Verify all tx links resolve publicly
  - Depends on: P1.8, P4.2 (testnet — critical path; add P1.9/P4.3 to this check too if the mainnet side track lands before submission)
  - Acceptance: every tx link in the submission opens on a public explorer with no auth required.
  - Evidence: 5/5 real tx hashes on record (P1.8 postEvent/mint/approve, P4.4 router-deploy/baseline-swap) confirmed HTTP 200 via `curl`, no auth, on the X Layer testnet explorer — 2026-08-17. **Found and fixed a real bug in the process**: the explorer URL recorded everywhere in this project (`https://www.okx.com/web3/explorer/xlayer-test`) 301-redirects to a different canonical path (`https://web3.okx.com/explorer/x-layer-testnet`) — both work in a browser, but the old form adds an unnecessary redirect hop and one `curl` check failed outright on it (transient, but avoidable). Corrected the constant in `apps/web/src/lib/chain/chain.ts` (the actual frontend code generating these links) plus `HACKATHON.md` and `DECISIONS.md`; `tsc --noEmit` re-confirmed clean after the change. Still to verify once P1.9/P4.3 (mainnet side track) land: their tx links, using this same corrected URL pattern.

- [ ] **P6.5** — Post from project X account mentioning @XLayerOfficial
  - Depends on: P6.3 · **[HUMAN-ONLY]**
  - Acceptance: post is live, from the account created in P0.1, mentions @XLayerOfficial (Event A mandatory requirement #4).

- [ ] **P6.6** — Submit Google Form
  - Depends on: P6.3, P6.5 · **[HUMAN-ONLY]**
  - Acceptance: form submitted before 2026-08-21 23:59 UTC; confirmation saved.

- [ ] **P6.7** — Final Event A eligibility checklist
  - Depends on: P6.6
  - Acceptance: all mandatory items confirmed — AI in product design ✓; deployed on X Layer **Testnet** ✓ (with the testnet-PoolManager-and-mock-tokens caveat stated, not hidden); **mainnet launch is explicitly NOT required for this checklist** — corrected 2026-08-17, the official FAQ places it "subsequently," after the deadline (REF-027); dedicated active X account ✓; submission post ✓; form by deadline ✓.

---

## 4. Event B task list (window 2026-08-22 → 2026-09-02, deadline 2026-09-02 23:59 UTC)

### Phase 7 — Orion registration & assets

- [ ] **P7.1** — Orion wallet-signature registration on Base
  - Depends on: none (can start any time in the window) · **[HUMAN-ONLY]** — non-refundable ~$10 ETH spend, TEAM.md already flags this as Dien-only
  - Acceptance: registration confirmed; only the wallet touches Base, the product stays on X Layer.

- [ ] **P7.2** — Confirm required Event B assets present
  - Depends on: P0.6
  - Acceptance: website, X profile, GitHub, Discord/Telegram all still live (reused from Event A).

### Phase 8 — Live NVIDIA earnings trace & sentinel A/B

- [ ] **P8.1** — Monitor the live 2026-08-26 NVIDIA earnings event
  - Depends on: P1.9, P4.3 (Event A pipeline must already be working)
  - Acceptance: complete trace captured — filing → parsed state → on-chain post → fee change → scoreboard entry — for the real earnings release (~20:20 UTC results, 21:00 UTC call per spec §7).

- [ ] **P8.2** — Sentinel A/B test
  - Depends on: P8.1
  - Acceptance: two equal small LP positions in the existing v3 wNVDAx pool, one passive, one feed-guarded (withdrawn/narrowed on a GRAVE-state signal).

- [ ] **P8.3** — Publish sentinel A/B results
  - Depends on: P8.2 · involves real fund movement — orchestrator confirms with Dien before treating as **[HUMAN-ONLY]** or executor-fundable from a pre-approved small pool
  - Acceptance: both P&Ls and all transaction links published after the earnings event, regardless of outcome.

### Phase 9 — Deferred-item backfill (time-permitting, in this priority order)

- [ ] **P9.1** — Extend reaction-latency + markout coverage with new filings
  - Depends on: P2.2, P2.4
  - Acceptance: incorporate filings that land during the Event B window (~1.6/day across the 10 names) into the existing studies.

- [ ] **P9.2** — Grade calibration against realized outcomes
  - Depends on: P1.5, P5.1
  - Acceptance: severity/direction grades compared against what actually happened to the reference price; published as a track record (spec §4.2).

- [ ] **P9.3** — Scoreboard backfill
  - Depends on: P5.1
  - Acceptance: historical entries added retroactively where reaction data exists.

- [ ] **P9.4** — Telegram bot (automate the static channel)
  - Depends on: P5.4, P5.3
  - Acceptance: same posting logic as the X bot (P5.3), pointed at Telegram.

- [ ] **P9.5** — Automatic index-reaction detection
  - Depends on: P5.1
  - Acceptance: scoreboard's reaction timestamps stop being operator-recorded and are detected programmatically from the P0.8 poller series.

- [ ] **P9.6** — ERC-8004 registration
  - Depends on: none
  - Acceptance: single CLI command run; not marketed as a headline feature (spec §3 — explicit anti-pattern from the winner corpus).

### Phase 10 — Event B submission assembly

- [ ] **P10.1** — Submission narrative
  - Depends on: P8.3
  - Acceptance: frames the event agent as the submitted AI agent, its on-chain effects as bonded registry updates that change live swap pricing.

- [ ] **P10.2** — Feature the NVIDIA trace + sentinel A/B as the differentiator
  - Depends on: P8.3, P10.1
  - Acceptance: explicitly contrasts against the two existing read-only analyst entries on Base (spec §2 field note) — this project *acts* on-chain, theirs don't.

- [ ] **P10.3** — Final Event B submission
  - Depends on: P7.1, P10.1, P10.2 · **[HUMAN-ONLY]**
  - Acceptance: submitted before 2026-09-02 23:59 UTC.

---

## 5. Deviations log

Record here, don't silently reinterpret a task. One line each: date, task ID, what changed, why, who approved it.

- **2026-08-17, P0.6 (GitHub repo scaffolding).** Dien decided `git init` should happen at the repo root (`/Users/scientivan/Programming/New`), accepting the stated tradeoff that other local projects (`.impeccable/`, `.superstack/`) live in the same directory. Implementation choice made by orchestrator, not separately re-confirmed: `contracts/` stays its own independent git repo (needed for Foundry's `forge install` submodule-based dependency management, already working — 56/56 tests passing) rather than being folded into the root repo; root `.gitignore` excludes it, `.impeccable/`, `.superstack/`, `node_modules/`, and `.env`. Root repo initialized locally (`git init` run, `.gitignore` written) but **not yet committed, no remote created, nothing pushed** — those are separate, more consequential steps deferred until explicitly requested or until P0.6/P6 actually need the repo live. If Dien wants `contracts/` folded into the root repo instead (single unified history, submodule surgery required), say so and this gets redone before the first commit.

---

## 6. Active assignments

Orchestrator writes one block per in-flight task here; delete the block once §7's verify-and-close step completes and the task's evidence line is filled in above.

```
Task:      [ID]
Executor:  [session name / "Dien" for HUMAN-ONLY]
Assigned:  [date/time]
Plan:      [pasted here, or "sent via SendMessage to <session>"]
Approval:  [pending / APPROVED — date / revision requested: <what>]
Status:    [planning / executing / done (unverified) / verified]
```

Task:      P1.6, P1.7, P4.1 (contracts)
Executor:  subagent "contracts-executor" (Agent tool, general-purpose, Sonnet 5)
Assigned:  2026-08-17
Status:    verified 2026-08-17 — orchestrator independently ran `forge build`/`forge test` (56/56 passing) and read every contract file directly; boxes checked above with evidence.

Task:      P1.1, P1.2, P1.3, P1.4, P1.5 (EDGAR pipeline)
Executor:  subagent "pipeline-executor" (Agent tool, general-purpose, Sonnet 5)
Assigned:  2026-08-17
Status:    verified 2026-08-17 — orchestrator independently ran `tsc --noEmit` and the full test suite (23/23 passing), read every source file directly, and personally resolved a stripped-text token-count discrepancy (not just trusted the executor's explanation) by running the stripper and reading its full output; boxes checked above with evidence. Live behavior (P1.1 continuous polling, P1.3/P1.5 real Gemini calls) still blocked on P0.11 (VPS) / P0.13 (Gemini key) — noted per-task above, not silently marked fully done.

Task:      P1.8 completion (wire real on-chain posting: viem client, off-chain↔on-chain field-shape and EventType-vocabulary mapping, run one real filing end-to-end, read it back)
Executor:  Sonnet 5 execution agent (dispatched with the full approved plan below)
Assigned:  2026-08-17
Plan:      Opus 5 planning agent returned a fully-verified plan (live-checked chain state and EDGAR, not assumed): viem@2.53.1 (const-ABI type safety); Gap 1 (array vs scalar) solved via the contract's own `extraDataURI`/`extraDataHash` escape hatch with a self-contained `data:` URI (no dead links to unhosted infra); Gap 2 (EventType vocabulary) solved with an explicit off-chain→on-chain mapping table keyed on eventType+form, explicitly flags `Form8K_Delisting` as currently unreachable rather than fudging a mapping to it, and explicitly forbids using unbonded severity.direction to pick insider buy/sell (would collapse the bonded/unbonded separation); Gap 3 (token address) verified by reading the contract that `token` is a pure label with zero external calls, uses real mainnet MSTRx address as label + a testnet/mainnet lookup table for future tasks that do need a resolvable testnet address (the hook demo, P4.4). Overrode the orchestrator's bondAmount=0 suggestion with a specific reason: zero bond means the contract's transfer branch never executes, leaving the "bond" stage of the acceptance criterion unexercised — recommends a real 10 mockUSDT0 bond instead (mint+approve automated, token is freely mintable). New files under `apps/server/src/chain/`, one new pure-unit test file for the mapping table, explicit fail-on-mismatch read-back (exit code 1), found and fixes a real bug along the way (`EDGAR_USER_AGENT` missing from `.env`, blocks EDGAR polling). Full plan text handed to the executor agent verbatim.
Approval:  APPROVED — 2026-08-17 (orchestrator; verified reasoning against spec §7 facts and the contract source, no contradictions found; all constraints respected — no contracts/ changes, no control-file edits, no frontend)
Status:    executing

Task:      P4.4 (end-to-end synthetic-injection test — high-severity + negative-control, real testnet swap)
Executor:  Sonnet 5 execution agent (dispatched — build-only phase today, live Gemini calls deferred to quota reset)
Assigned:  2026-08-17
Plan:      Opus 5 planning agent live-verified everything against the actual deployed contracts and corrected two errors in the orchestrator's own brief: (1) the brief's poster/relayer addresses were stale (from wallets the orchestrator generated early on, before Dien's OKX-wallet addresses became the real funded ones) — corrected to the real `.env` addresses; (2) real Gemini call count is 8 (not 6) per full run, since `runPipelineForFiling` also calls `gradeFilingSeverity` in the same `Promise.all`. **Overrode the orchestrator's own negative-control suggestion with empirical proof it was wrong**: a "routine low-severity" filing does NOT stay near baseFee in this system — P1.8's real live run already proved `declaredAmountAgreement` lands at 0 in practice (3 parses rarely produce byte-identical amount labels), which triggers `concernTier`'s weak-consensus +1 bump on almost any real filing, pushing even "routine" events to 7000-20000 fee (14-40x base). Redesigned the negative control around the actual mechanism that suppresses posting — the `readyToPost` consensus gate — with a pre-registered fallback if the parses unexpectedly agree (report and escalate, never retry with a different document). Found that reusing the existing `runOnce.ts` entrypoint would create a genuinely fabricated bonded claim (a fake sourceUrl claiming to be a real EDGAR document) — designed a new entrypoint with self-identifying markers instead (`synthetic://` URL scheme, `SYNTHETIC-P4.4-*` accession numbers, an HTML-comment warning banner that's part of the hashed content but invisible to the parser). **Found and escalated the P5.3 hazard** now recorded permanently on P5.3's own tracker entry. Full swap-verification design with a demo-legible headline number (visibly smaller `amountOut` on the widened-fee swap). No new Foundry tests needed (existing 56 already cover the exact fee-clamp scenarios); new TS unit tests for the new off-chain logic only (`expectedFee.ts` pure fee-policy reimplementation, pinned against Foundry's own test vectors).
Approval:  APPROVED — 2026-08-17 (orchestrator; rigorous, live-verified, correctly overrides my own flawed suggestion with evidence, no contradictions with established facts or deployed contract behavior)
Status:    executing (build-only phase — live Gemini parses deferred to quota reset, sequenced BEFORE P2.1's daily batch since P4.4 gates the Phase 6 demo/submission path and P2.1 does not)

Task:      P2.4 (markout study — realised LP loss on P2.2's identified first-trades)
Executor:  Sonnet 5 execution agent (dispatched with full approved plan)
Assigned:  2026-08-17
Plan:      Opus 5 planning agent live-verified everything and ran a real one-event dry run before finalizing (reproduced P2.2's gap_seconds=222 exactly, decoded a real Swap log, computed a real markout of −$0.12 @ 60min). Formula: direct realised markout from Swap-event deltas (dU + dS×P_h, no external price feed needed — argues explicitly that concentrated liquidity does NOT require an approximation here, a genuine methodological finding, not a hedge). Found two more real bugs/gaps: (1) token0/token1 ordering is NOT uniform across the 10 reference pools — MSTR and COIN have USDG as token1, not token0 like the other 8; hardcoding the assumption would have silently inverted the price for 2/10 pools; (2) the MSTR pool wasn't deployed until 2026-07-22, so P2.2's "no trade" event at 2026-07-20 is actually "no pool yet" — recommends re-checking all 14 P2.2 no-trade events for this pattern. Scope: all 32 of P2.2's 46 events that had a qualifying first trade (not just NVDA's n=2), TVL-at-event-block as the denominator (not current TVL — AMZN's pool is down to $19 today but had real liquidity during its P2.2 window). Extensive pre-registered limitations section (lower-bound not total, aggregate-only not per-LP, no significance claims below n≈4, explicitly warns not to claim a big number if the honest one is small — ties back to spec §9's own pre-commitment). Correctly designed to flag cross-file corrections (to reaction-latency-study.md, spec §9) to the orchestrator rather than silently auto-editing.
Approval:  APPROVED — 2026-08-17 (orchestrator; rigorous, live-verified, no contradictions with established facts, extends rather than duplicates P2.2)
Status:    executing

- **2026-08-17, wallet generation for P0.2/P0.4/P0.11/P6.2.** Orchestrator generated two wallets via `cast wallet new` (poster key, demo-relayer key — deliberately separate per spec, so the demo relayer can never also post events). Private keys saved only to `/Users/scientivan/Programming/New/.secrets/` (chmod 600, directory chmod 700, confirmed `.gitignore`d before any `git add` touched the repo). Only the public addresses are recorded here for Dien to fund:
  - Poster wallet: `0x2139b8E4AdB0755cE1776717016b1Ff8Fabac4E2`
  - Demo-relayer wallet: `0x0d6d05e9AEE5e21dE241299Cabd566a3B1F1d732`
